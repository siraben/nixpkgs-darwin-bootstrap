# Darwin Bootstrap Todos

## Roadmap / Current Status

- [x] Primary path is amd64 Darwin: the Nixified Mach-O chain now reaches GCC 4.6.4 C/C++, GCC 10.4.0, nixpkgs-matched `gcc_latest`, and strict phase47 `gcc_latest`.
- [x] Current package proof is GNU Hello 2.12.2 built by phase46, strict phase47, and nixpkgs `gcc_latest`; `gnu-hello-hash-comparison` is the formal validation entrypoint.
- [ ] Purity hardening remains: phase44/phase45/phase46 shortcuts, CLT SDK paths, Apple `/usr/bin` tools, nixpkgs `gnumake`, and compiler-only sysroot/wrapper packaging are explicit impurity boundaries.
- [ ] Promote `phase39-gnumake` beyond smoke-test status: it executes simple recipes, but a direct GNU Hello/Automake build segfaults while considering generated header targets.
- [ ] Next promotion target is a bootstrap-appropriate Darwin `stdenv.cc` boundary with complete bintools, SDK/sysroot, runtime libraries, `nix-support`, and fewer host shortcuts.
- [ ] aarch64 remains a follow-up track with smoke/template coverage and a signed `hex1` candidate only; defer trusted-chain work until the amd64 boundaries stabilize.

## Current Status Update

- [x] Phase44 Nix formalization now builds a GCC 4.6 C/C++ handoff; it still temporarily uses nixpkgs `gnumake` as `BOOTSTRAP_MAKE` until phase39 make is fixed beyond smoke tests.
- [x] Phase44 Nix reaches `cc1`/`cc1plus`, `g++`, direct target `libgcc`, direct target `libstdc++`, packaging, and the C++ smoke assembly check with explicit Mach-O `as`/`ld` wrappers.
- [x] Phase44 packaging now installs `as`, `ld`, and `collect-ld` wrappers into both `bin/` and the GCC runtime `-B` directory so the packaged `gcc.real` can compile during phase45 Nix builds.
- [x] Phase45/46/47 Nix formalization now declares `perl` as a native tool, stages the input compiler sysroot into a writable build-local copy, passes a deterministic CLT SDK path, isolates build-machine tools from bootstrap sysroot headers, preserves GCC generated lexer/parser outputs without flex/bison, patches compiler-only fixincludes probes in `gcc/Makefile.in` before `gcc/` is configured, host-shortcuts only ABI-neutral generated GCC sources for the phase45 formalization boundary, extends phase46 support-library host shortcuts to anonymous Nix build directories, and temporarily uses nixpkgs `gnumake`.
- [x] Nix phase35 GCC 4.6 `all-gcc` builds cleanly from the extracted source-prep script after removing the stale driver option and dumpbase failure paths.
- [x] Nix phase36 GCC 4.6 `libgcc` now builds with the direct `cc1` wrapper, Darwin `struct stat` ABI repair, staged configure inputs, and the bootstrap assembler path.
- [x] Embedded Python phase scripts have been extracted into `scripts/`; keep future phase logic in separate script files rather than heredocs in Nix.
- [x] Strict phase47 `gcc_latest` self-build now completes `all-libcody all-gcc` with `GCC_MODERN_WRAPPER_HOST_SHORTCUTS=0`, packages `phase47-gcc-latest-strict-bootstrap`, and passes C/C++ Mach-O smoke links through its own wrappers.
- [x] GNU Hello 2.12.2 now builds at `-O2 -g0` with the strict phase47 `gcc_latest` handoff and runs `Hello, world!`, `--version`, and `--help`; the formal proof is `gnu-hello-gcc-latest-strict`.
- [x] Freeze the strict phase47 recipe into Nix and rerun from a clean store output instead of the current stabilized impure tree.
- [x] Compare the strict phase47-built GNU Hello bytes against a nixpkgs GCC-built GNU Hello after adding a same-source/same-flags nixpkgs reference build; phase46 and strict phase47 are byte-identical, while nixpkgs GCC 15.2.0 differs.
- [x] Retarget the bootstrap `gcc-latest` handoff to nixpkgs `gcc_latest.version` so GNU Hello comparisons use matching GCC major/minor versions; `gnu-hello-hash-comparison` is the validation entrypoint.
- [x] Phase45 GCC 10 now packages a compiler-only handoff from the repaired `all-gcc` tree for phase46 iteration.
- [x] Phase45 GCC 10 wrapper handles normal Mach-O compile/link smoke tests, configure `conftest` probes, and impure phase46 support-library source compiles without falling back into the slow/hanging transitional `cc1` path.
- [x] Phase46 source patching now normalizes `libiberty/physmem.c` Darwin header guards idempotently, including `machine/hal_sysinfo.h`, `sys/table.h`, and `sys/systemcfg.h`.
- [x] Phase46 configure now correctly marks missing Darwin `process.h` as absent; the prior false positive came from the wrapper's fake preprocessor path.
- [x] Phase46 no longer spends GCC10 `cc1` time on bootstrap-unneeded `fixincludes`; top-level fixincludes recursion is no-opped and the wrapper has an impure host shortcut if it is still reached during iteration.
- [x] Phase46 now reaches GCC's own `Makefile`; the next blocker was a stale `LIBBACKTRACE` prerequisite despite `--disable-libbacktrace`, so the compiler-only handoff strips that dependency from `gcc/Makefile`.
- [x] Phase46 GCC frontend now skips `stmp-fixinc` directly by clearing `STMP_FIXINC`; no compiler-only handoff should depend on generated fixed system headers.
- [ ] Phase46 impure discovery currently compiles GCC frontend sources and final host C++ links with host clang through the wrapper to avoid multi-minute GCC10 `cc1plus` iterations; this must be narrowed or removed before freezing a bootstrap-pure phase.
- [x] Phase46 impure `all-gcc` for nixpkgs-matched `gcc_latest` completed from the GCC 10 handoff and packages a smoke-tested compiler-only handoff.
- [x] Strict phase47 `gcc_latest` self-build with wrapper host shortcuts disabled passed the GCC frontend boundary.
- [x] GNU Hello `-O2` built by strict phase47 `gcc_latest` runs successfully; the earlier exit 139 was fixed by making the bootstrap sysroot expose Darwin-compatible stdio/fcntl details and by linking wrapper default runtimes.
- [ ] Reuse nixpkgs GCC infrastructure at the recipe boundary first: minimal-bootstrap source/dependency/configure shape plus gcc-ng compiler-only archive seeding, not full `stdenv.mkDerivation` until the phase46 compiler is promoted to a complete runtime/wrapper closure.
- [ ] Stop treating the modern GCC phase like the early GCC46 phase: switch stabilized GCC15/latest iteration to external nixpkgs-style GMP/MPFR/MPC/zlib and the full Apple SDK sysroot before trying to prove strict GNU package builds.
- [ ] Promote the phase46 compiler-only handoff into a bootstrap-appropriate Darwin `stdenv.cc` boundary with complete `nix-support`, bintools, SDK/sysroot, `libgcc`, and `libstdc++` metadata before switching to full nixpkgs GCC machinery.
- [x] `plan.md` has moved to `todos.md`; keep this file as both the task list and running bootstrap log.
- [x] Phase44 no longer reruns seeded GMP/MPFR/MPC/libiberty/fixincludes/zlib/libcpp/libdecnumber work during impure iteration.
- [x] Phase37 wrapper now stages GCC source-neighbor directories, rewrites temp dependency paths, and handles nested GCC `config/...` includes.
- [x] Phase44 now seeds GCC generated MD/GType outputs and overrides fragile generator stamps including `s-gtype`.
- [x] Rebuilt the impure phase37 wrapper against the live phase34 TinyCC store path.
- [x] Patched phase44 to seed/touch phase35 `build/gen*` binaries and override generator binary rebuild targets after `genmodes`/`genmddeps` relinking hit bootstrap assembler label gaps.
- [x] The phase44 retry moved past generator binary linking and into GCC frontend compilation.
- [x] Rebuilt the impure phase37 wrapper with the include-dir `config/` overlay fix and directly compiled `g++spec.o`.
- [x] Directly validated the current phase37 wrapper/source-overlay fix by compiling `c-family/stub-objc.c` through the TinyCC-derived GCC wrapper.
- [x] Added fast impure iteration controls: phase44 resume, phase44 per-target make, and phase37 self-test skipping for wrapper-only rebuilds.
- [x] Added an impure Mach-O object mode to the phase37 GCC wrapper and validated `c-lang.o` plus `c-family/stub-objc.o` as Mach-O x86_64 objects.
- [x] Removed the broad phase44 source-tree header symlink overlays; include repair now happens inside the phase37 wrapper temp tree.
- [x] Fixed wrapper temp-tree layout for GCC source-relative includes, including `../libcpp/internal.h`, `all-tree.def` references into `c-family/`, and nested `config/.../config/vxworks-dummy.h`.
- [x] Validated the next Mach-O targets: `c-family/c-ppoutput.o`, `i386-c.o`, and `darwin-c.o`.
- [x] Fixed generated plain GCC sources such as `insn-attrtab.c` by staging non-`conftest.c` local inputs through the same temp include overlay.
- [x] Validated `insn-attrtab.o` as a Mach-O x86_64 object.
- [x] Added an env-gated impure host-CC shortcut for huge generated `insn-*.c` GCC sources while preserving the wrapper-owned Mach-O path for normal sources.
- [x] Validated `insn-emit.o` and `ggc-page.o` as Mach-O x86_64 objects under the resumed phase44 build.
- [x] Added a broader `GCC46_BOOTSTRAP_HOST_CC_SOURCES=1` impure discovery mode after full bootstrapped `all-gcc` iteration proved too slow.
- [x] Validated `caller-save.o` as a Mach-O x86_64 object through the broader impure host-CC source shortcut.
- [x] Fixed host-CC discovery mode to suppress Apple clang warning-errors that left `dwarf2asm.o`, `dwarf2out.o`, `opts.o`, `tree-sra.o`, and `tree-ssa-uninit.o` missing.
- [x] Fixed GCC 4.6 Mach-O runtime blockers found after `all-gcc`: Darwin stdio globals now initialize from libSystem at load time, phase34 `struct stat` matches Darwin's legacy x86_64 ABI, and `xgcc` emits Mach-O objects.
- [x] Patched the GCC 4.6 driver path for Mach-O bootstrap iteration: default bad host deployment targets are forced back to `10.8`, compile-and-link cc1 invocations use temp-path `-auxbase-strip`, and specs can bypass crashing `collect2` in favor of the Mach-O linker.
- [x] Validated current impure GCC 4.6 smoke path: `xgcc -c` produces a Mach-O x86_64 object, and `xgcc -isysroot … -nostartfiles -nodefaultlibs … -lSystem` links a Mach-O executable returning 42.
- [x] Finished the direct phase44 target `libgcc` build with `xgcc` emitting Mach-O objects; `libgcc.a` and an intentionally empty bootstrap `libgcov.a` copy back into `build/gcc`.
- [x] Minimized the current GCC 4.6 Mach-O patch set: keep temp `%g` auxbase paths, disable crashing target debug info, disable bootstrap-unneeded `libgcov`, and skip Darwin/i386 CRT extra-parts while the compiler/runtime boundary is still forming.
- [x] Direct target `libstdc++-v3` now builds to a Mach-O static `libstdc++.a` with the GCC 4.6 `xgcc`/`g++` pair.
- [x] Rebuilt target `libstdc++` against C++-safe phase34 headers and removed stale phase37 C header overlays from the GCC source include path.
- [x] Installed the direct `libstdc++`/`libsupc++` outputs into the phase44 impure prefix and smoke-tested `g++` compiling/linking a `std::string` Mach-O executable.
- [x] Folded the direct target `libgcc`/`libstdc++` recipe and manual GCC output packaging into `scripts/gcc46/phase44-cxx.sh`.
- [x] Added and validated `PHASE44_SKIP_MAIN_MAKE=1` so the direct runtime/package path can be rerun against a resumed phase44 tree without replaying `all-gcc`.
- [x] Phase45 GCC 10 `xgcc` now survives `-dumpspecs` and `-print-sysroot-headers-suffix`; the bootstrap script patches the Darwin terminal-width crash and makes the sysroot suffix probe non-fatal.
- [x] Phase45 resume mode now preserves already-built host helper archives instead of deleting `libcpp.a`/host support on every per-target retry.
- [x] Phase45 C++ frontend link now uses C++-safe bootstrap headers for `times`, avoiding the mangled Darwin `__Z5timesP3tms` runtime reference.
- [x] Phase45 skips GCC 10 bootstrap-only macro/fixincludes/selftest probes that hang under the transitional `xgcc` path.
- [x] Phase45 target configure now pre-seeds Darwin x86_64 size/decimal/fixed-point facts to avoid intentional autoconf probes that can spin under transitional `xgcc`.
- [x] Phase45 target `libgcc` configure now hard-codes the Darwin x86_64 `host_address=64` result and disables decimal float before configure can invoke another spinning `xgcc -E conftest.c` probe.
- [x] Tightened the phase45 `libgcc` configure rewrite after the first pattern missed GCC 10's exact generated shell text; the live impure source now contains only `host_address=64`.
- [x] Phase45 target configure now forces non-glibc `0.0` detection instead of compiling `<features.h>` probes through transitional `xgcc`.
- [x] Phase45 GCC 10 host-helper regeneration now preserves the Darwin SDK fixes in `auto-host.h`/`config.status`/`config.cache` so `rlim_t` and `strsignal` do not regress during resumed `all-gcc`.
- [x] Phase45 `libgcc` target configure now pre-seeds AVX/LSE/init-priority feature results to avoid final target compile probes that can spin under transitional `xgcc`.
- [x] Phase45 Darwin `libgcc` now drops bootstrap-unneeded `libemutls_w.a` and caches `gcc_cv_use_emutls=no` so disabled-shared builds do not ask for missing `emutls_s.o`.
- [x] Previous phase45 blocker resolved for the modern handoff path: full install is bypassed for now, and the compiler-only package is used as the phase46 input.
- [x] Current phase44 blocker resolved: the full scripted phase44 path validates from a clean Nix build root and packages the GCC 4.6 C++ handoff.

## Running Log

- 2026-05-23: Task #12 LANDED for phase47 strict. Goal — promote GMP/MPFR/MPC/ISL from in-tree builds under phase45/46/47 to standalone Nix-store derivations built from the bootstrap compiler chain — achieved end-to-end. New derivations:
    - phase26c-bootstrap-gmp   (GMP 6.3.0, built by phase46)
    - phase26d-bootstrap-mpfr  (MPFR 4.2.2, depends on phase26c)
    - phase26e-bootstrap-mpc   (MPC 1.3.1, depends on phase26c+d)
    - phase26f-bootstrap-isl   (ISL 0.24, depends on phase26c)
  Each ships `$out/include/<name>.h` + `$out/lib/lib<name>.a` and runs a smoke link. bootstrap-gcc.sh now picks up `GCC_MODERN_EXTERNAL_GMP/MPFR/MPC/ISL` env vars; when set, it appends `--with-gmp=…` etc. to GCC's configure flags AND removes the in-tree `gmp/`, `mpfr/`, `mpc/`, `isl/` directories from the source tree so the external paths win unambiguously. Phase45 and phase46 still use in-tree libs because they are the compilers used to build phase26c-f (cycle break); phase47 strict consumes the externals — that's where the bit-equivalent GNU Hello proof lands. `nix build .#packages.x86_64-darwin.gnu-hello-hash-comparison` reproduces phase46 == strict phase47 at hash `5019a64510837fae43fc7238b506ec11011542432c792b4ab7683db2e7ff2f73` post-switch, identical to the pre-task baseline. CFLAGS knobs required for GMP 6.3.0 + MPFR/MPC/ISL to build through GCC 15.2: `-std=gnu99 -Wno-implicit-function-declaration -Wno-implicit-int -Wno-int-conversion -Wno-incompatible-pointer-types -Wno-return-type` (configure conftests are K&R-era).
- 2026-05-22: Task #12 (external GMP/MPFR/MPC for modern GCC) first attempt blocked by the same root cause as #13. Tried `phases/phase26c-bootstrap-gmp.nix` building GMP 6.3.0 standalone using two candidates:
  - phase44 (gcc46-cxx): `ld: library not found for -lgcc_ext.10.5` — phase44's Darwin specs reference a legacy compat lib we never built/shipped.
  - phase45 (gcc10): `stddef.h:27: unterminated #if`, `limits.h:34: syslimits.h: No such file or directory` — phase45's compiler-only handoff bundles incomplete headers that work for the chain's internal self-builds (with the wrapper overriding paths) but break external `configure` tests that invoke the compiler directly.
  Root cause: our phase44–47 GCCs are compiler-only handoffs (frontends without complete runtime/header closures), so they can't build external software. Fixing task #13 (have phase46 ship its own libgcc + libstdc++ + complete include trees) unblocks #12.
  The WIP `phase26c-bootstrap-gmp.nix` is kept in-tree as a placeholder; the build switches to phase45 today, the comment block documents the next step.
- 2026-05-22: Goal #14 (replace tools/elf64-to-m1.py with hand-written M1) started. Added `tools/elf64-to-m1.M1` skeleton + `phases/phase26b-elf64-to-m1.nix` that assembles it through phase9-m1 + phase10-hex2 (no python in *that* phase's closure). M1 macro expansion and hex2 assembly both succeed; the resulting Mach-O binary fails codesign because the existing chain relies on `tools/phase5-amd64-m2.py patch` (used by 13 phases) and `tools/phase4-amd64-cc-arch.py patch` (used by phase4) to back-fill Mach-O segment sizes after hex2 — those patchers are how every signed binary in the chain (kaem, M2-Planet, TinyCC, etc.) gets correctly-sized __LINKEDIT segments. Removing python3 from the closure for elf64-to-m1 transitively requires replacing the Mach-O segment-size patcher too. Python tool inventory: elf64-to-m1.py (375 lines, 9 phases), hex2-data-relocs.py (112, 4), m1-to-hex2.py (92, 1), patch-macho-large-segments.py (64, 1), phase1-amd64-hex1.py (252, 1), phase2-amd64-catm.py (121, 1), phase2-amd64-hex2.py (267, 1), phase3-amd64-m0.py (116, 1), phase4-amd64-cc-arch.py (169, 1), phase5-amd64-m2.py (137, 13). All ten tools (~1600 LoC of Python) form the Darwin Mach-O wrapper layer that adapts stage0's ELF-targeted outputs to Mach-O.
- 2026-05-24: Task #14/#15/#16 — hand-written M1 elf64-to-m1 makes structural progress. New `phases/phase26b-elf64-to-m1.nix` now successfully builds, the `tools/elf64-to-m1.M1` skeleton (~370 lines) compiles via the existing M1+hex2 chain, codesign is skipped (the Mach-O segment-size patcher cycle is documented as the remaining python dep, but the Darwin nix-sandbox loader runs unsigned binaries). The minimal binary works end-to-end: `./elf64-to-m1` → "elf64-to-m1: not yet implemented" → exit 42 (placeholder). Argv loop + usage path also lands: `./elf64-to-m1` (no args) → "usage: ..." → exit 64. ELF magic check has a SIGBUS bug at the lea_rax/cmp_imm32 sequence; without lldb workflow on the unsigned binary, next iteration needs a single-step bisect via FUNCTION_write_stderr printf-style traces. Helpers committed: FUNCTION_exit, FUNCTION_write_stderr/stdout/fd, FUNCTION_strlen, FUNCTION_open_ro, FUNCTION_close, FUNCTION_read_n, FUNCTION_mmap_anon, FUNCTION_argc, FUNCTION_argv. Future-work stubs: parse_args, load_input, parse_elf_header, parse_sections, parse_symbols, parse_relocations, build_labels, emit_output, write_output. amd64_defs.M1 missing many register-move encodings (mov_rdx,rsi / mov_rsi,rdi / sub_rax,rdi / js / etc.) — used raw `!0xXX` bytes with the expected encoding noted in comments. Task #16 (Nix phase) is complete; task #15 (M1 program body) and tasks #17-19 (downstream wiring) need the full ELF parser + M1 emitter, estimated 1500+ more lines of M1 plus debugger workflow setup.
- 2026-05-24 (later): Task #15 lands a working hand-written elf64-to-m1: tools/elf64-to-m1.M1 grows from ~470 → ~1900 lines and now: opens/mmaps the input ELF, validates the header, loads the section header table, identifies & loads .text/.rela.text/.symtab/.strtab/.data into separate mmap'd buffers, walks the symbol and relocation tables, pre-mutates GOT-load bytes from 0x8b (mov) to 0x8d (lea) for R_X86_64_GOTPCREL/GOTPCRELX/REX_GOTPCRELX relocations, emits a python-equivalent M1 source stream with: byte literals (`!0xHH`), symbol references at relocation sites (`%[prefix]name` with bind-aware prefixing — LOCAL gets the --prefix, GLOBAL/WEAK do not — matching python label_name semantics), label definitions at symbol offsets (`:[prefix]name`), synthetic `_plus_<hex>` intermediate labels for GLOBAL/WEAK symbols at addends {1, 2, 8, 16, 24, ...} up to sym_size, `:ELF_data` / `:HEX2_data` section markers, and .data section emission with its own labels. Also handles `--prefix <value>` and 2nd-positional output-path argv parsing; opens output file with O_WRONLY|O_CREAT|O_TRUNC and threads the fd through all emit calls via `[rbp - 144]`. Name cleaning (FUNCTION_emit_cleaned_name) implements the python label_name regex escaping: '.LC0' → 'ELF__LC0', etc.
- 2026-05-24 (later): Task #17 (replace python3 elf64-to-m1.py invocations) — phases switched: **phase27-tinycc-elf-to-macho-probe**, **phase28-tinycc-self-m1-probe** (the 17k-line tcc.o conversion!), **phase29-tinycc-sysv-libc-probe** (hello.o with .data L.0 symbol). All three build end-to-end with the python-free emitter; phase27's emitted Mach-O runs and returns 42 as expected. Remaining phases using python elf64-to-m1.py: phase30-tinycc-self-link-candidate, phase31-tinycc-self-compile-probe, phase33-tinycc-boot1-link-candidate, phase34-tinycc-darwin-cc, tinyccSelfLinkCandidate.nix. Phase30 fails on `tcc_self_tok` — the `tok` symbol is in .bss (or SHN_COMMON) which the current emitter doesn't handle. Next iteration needs: .bss section emission (with padding from python `align()`), .rodata section emission (similar to .data), R_X86_64_PC32 / PLT32 self-text computed-displacement emission (instead of always-symbol-ref), R_X86_64_64 8-byte slot emission (`&LABEL_sym !0x00 !0x00 !0x00 !0x00`), and R_X86_64_32 / 32S 4-byte slot emission (`&LABEL_sym`). The emitter has been driven through 30+ working commits this iteration (5a9e7d0..bc3796b in the log); each piece (ELF header → section table → shstrtab → section identify → load → symtab walk → relocation walk → GOT mutation → byte emit → label emit → synth labels → prefix → name cleaning → file output → .data) was verified by readelf/xxd comparisons before moving on.
- 2026-05-24 (latest): Task #15 — added .bss section emission (3 new stack slots [rbp-192]/-200/-208 for sh_size, idx, sh_addralign, with 3 extra push_rax at function entry to actually allocate them — the previous draft was writing below rsp which got clobbered). Pre-pass aligns .data->.bss boundary then emits ':<prefix><name>' labels for shndx==bss_idx matches and `!0x00` bytes for the full bss_size. Also added R_X86_64_64 emission (`&<sym>` + 4 `!0x00` byte literals, advance 8 bytes) via new FUNCTION_emit_abs64_ref. Phase28 now produces the right shape (`:tcc_self_tok` defined, 2.6 MB output matching tcc.o's 0x24888-byte .bss); M1+hex2 succeed end-to-end on the combined phase30 file. But the resulting tcc-self binary segfaults on `-version` — remaining gap is R_X86_64_PC32 self-text computed displacement. Python emits `emit_i32(sym_value + addend - r_offset)` (a literal 4-byte signed displacement) when the relocation's target sits inside the same section; our emitter always falls through to `%<sym>` which M1 resolves to a 32-bit pc-rel to the LABEL, but without the addend baked in we get an off-by-(addend) displacement on every self-text call/jump. Until that's implemented, phase28 stays wired to python so gnu-hello-hash-comparison stays at baseline 5019a64510837fae43fc7238b506ec11011542432c792b4ab7683db2e7ff2f73. Total of ~37 commits this session iteration, tools/elf64-to-m1.M1 now ~2100 lines.
- 2026-05-24 (further): Task #15 continuation — added PC32 self-text computed displacement (FUNCTION_emit_i32 + dispatch r_type==2/4 → if sym.shndx==text_idx compute sym_value+r_addend-r_offset, else fall back to %label_ref), R_X86_64_32/32S emission (`&<sym>` 4-byte, FUNCTION_emit_abs32_ref), and the LOCAL '_<sym_idx>' suffix that python appends to bind==0 labels (FUNCTION_emit_dec_lz writes decimal-no-leading-zeros). All five emit functions (emit_label_def/ref, emit_synth_label, emit_abs32_ref, emit_abs64_ref) now take rdx=sym_idx and emit the suffix when skip_prefix==0. Also added .rela.data section identification + load + per-byte relocation dispatch in DATA_emit_loop (mirrors RELOC_check_loop in .text emit). Quantitative diff on tcc.o: byte literals match exactly (419741 == 419741), refs match (3350 == 3350), abs refs match (63 == 63). Only labels diff: ours 2238 vs py 2251 (13 short). The 13 missing are python's dynamic addend-driven synth labels (':<sym>_<idx>_plus_<sym.value+effective_addend hex>') registered by symbol_label() in the relocation pre-pass for PC32-family relocs with effective_addend (addend+4) != 0 targeting .bss symbols. Implementing requires a per-byte cross-section scan with PC32 +4 adjustment and dedup — currently per-byte naive walks generate duplicates and miss the +4 offset. Phase30's tcc-self still SIGSEGVs (those 13 labels mean per-table-offset string lookups in tcc resolve to wrong addresses). Phase28 wiring stays on python. ~42 commits total this session iteration; tools/elf64-to-m1.M1 ~2500 lines.
- 2026-05-24 (finally): Task #15 — added DSYNTH_TXT_loop + DSYNTH_loop and BSYNTH_TXT_loop + BSYNTH_DAT_loop cross-section synth passes. Each scans both rela.text and rela.data per emitted byte, computes effective_addend (r_addend for ABS types 1/10/11, r_addend+4 for PC-rel types 2/4/9/41/42), and when target sym.shndx matches the section being emitted AND sym.value + effective_addend == rcx, calls emit_synth_label(name, target_offset, skip_prefix, sym_idx). On tcc.o now: unique label names match python EXACTLY (2251 unique vs 2251 unique); byte literals/refs/abs refs all match exactly. Total emitted labels are 2293 in ours vs 2251 in py — 42 duplicates from naive per-byte scan without dedup (M1 handles redefinition idempotently when address is the same). Phase30 still SIGSEGVs though, because the REFERENCE-side of relocation emission still emits base names (`%tcc_self_cstr_buf_73`) where python emits the synth-suffixed name (`%tcc_self_cstr_buf_73_plus_24658`). The fix is to thread target_offset through emit_label_ref/emit_abs32_ref/emit_abs64_ref so they append `_plus_<hex>` when effective_addend != 0 — adds a 4th arg to all three functions and requires the RELOC dispatch (text + data) to compute effective_addend at the call site. Total ~48 commits this session iteration; tools/elf64-to-m1.M1 ~2800 lines. Once the ref-side synth suffix is added, phase28→phase30 should unblock and ~6 more phases can be switched off python.
- 2026-05-24 (huge win): Task #15 + #17 — ref-side synth suffix landed. emit_label_ref / emit_abs32_ref / emit_abs64_ref now take a 4th argument rcx=target_offset and append '_plus_<target_offset_hex>' when nonzero (matching python symbol_label's synth path). Both RELOC_check_match (text emit) and DRELOC_match (data emit) compute effective_addend (with +4 PC-rel adjustment) before dispatch, pass rcx = (effective_addend ? sym.value+effective_addend : 0) to the emit function. Token-level diff vs python on tcc.o is now structurally equivalent — only 36 extra duplicate label defs remain (M1 takes the latest definition idempotently when address matches). **Phase30 builds end-to-end with ./tcc-self -version returning 0 and the '0.9.28-darwin-bootstrap' version string** — the first complete phase swap from python3 elf64-to-m1.py to our hand-written M1 binary in the tinycc bootstrap chain.  Then switched phase30/31/33/34/tinyccSelfLink to phase26b-elf64-to-m1 as well; phase34-tinycc-darwin-cc and its full closure (phase32/35/36/37/38) all build with the python-free emitter. With this commit, ALL phases in the tinycc bootstrap layer (phase27 through phase38) are off python3 elf64-to-m1.py. ~54 commits this session iteration; tools/elf64-to-m1.M1 ~2950 lines. Remaining python tools (phase5-amd64-m2.py, hex2-data-relocs.py, phase1-4-amd64-hex1/2/m0/cc-arch, etc.) are separate Mach-O segment-size patchers — out of scope for task #14/#15.
- 2026-05-25: Task #15 + #17 final — added '--symbols' mode to tools/elf64-to-m1.M1. Argv parser recognizes --symbols (no value), sets a flag at [rbp-240]. After section load, if flag set, SYM_LIST_pass walks the symtab and emits 'D\t<name>\n' or 'U\t<name>\n' per non-LOCAL non-empty symbol, then exits 0 (skips the convert path entirely). Output is raw stream; phase37-driver.sh pipes through `sort -u` for python-equivalent ordering (D=0x44 < U=0x55 sort separately by category). Phase37-gcc46-bootstrap.nix switched from python3 + tools/elf64-to-m1.py to phase26b-elf64-to-m1. Verified diff-empty against python on hello.o (1 sym) and tcc.o (88 unique syms after sort). **ZERO callers of python3 elf64-to-m1.py remain in the repo.** Task #14 / #17 effectively done — only remaining gap is dedupping the ~36 redundant label definitions emitted by our naive per-byte synth-label scan (M1 takes them idempotently when address matches, so functional but verbose).
- 2026-05-25: **Tasks #14, #15, #17, #18, #19 ALL COMPLETED.** Full chain `gnu-hello-hash-comparison` produces baseline `5019a64510837fae43fc7238b506ec11011542432c792b4ab7683db2e7ff2f73` with the entire tinycc bootstrap (phase27-phase38 + phase37 libgcc-symbols) using the hand-written `tools/elf64-to-m1.M1` (3257 lines) instead of `python3 tools/elf64-to-m1.py`. `phase46_phase47_equal=yes`. Byte-identical GNU Hello binary — the formal proof that the python-free path is functionally equivalent to the python-driven baseline. tools/elf64-to-m1.py is now dead code in the closure (still kept for reference but no derivation calls it).
- 2026-05-25 (cont): Begun full python tool replacement effort. Tasks #20-28 track each remaining python tool. **#20, #25, #26 COMPLETED** — all replaced with static templates + bash+sed+perl ports (proving the pattern works for tools that do header-gen + source-patch + assemble):
  - #20 patch-macho-large-segments.py → MACHO-amd64-largedata.hex2 template (7-line awk substitution on phase3-emitted lowdata; bakes hardcoded constants).  phase34 dropped @PYTHON@ @MACHO_LARGE_SEGMENTS@.  Also closed a leak in #17: phase34's runtime tcc-darwin-cc wrapper was still using python3 elf64-to-m1.py for every C compile; switched to phase26b binary.
  - #25 phase3-amd64-m0.py → tools/templates/MACHO-amd64-lowdata.hex2 (static committed) + scripts/stage0/port-m0-darwin.sh (bash+perl+sed port with 5 Darwin syscall patches).  Byte-identical to python output.
  - #26 phase2-amd64-catm.py → tools/templates/MACHO-amd64-catm-header.hex2 (unsigned header) + scripts/stage0/port-catm-darwin.sh (8 Darwin patches).  Byte-identical body bytes; phase2-catm builds + smoke test passes.
- 2026-05-25 (Tier B start): #22 phase5-amd64-m2.py + #23 hex2-data-relocs.py + #27 phase2-amd64-hex2.py patch step all share the same algorithmic core (read hex2 source, find static-data label, copy static block to DATA_FILE_OFFSET, RIP-rel32 displacement patching).  Created tools/macho-patcher.M1 + phases/phase26g-macho-patcher.nix as the foundation for a generic M1 macro-asm Mach-O patcher tool that handles all three.  Skeleton (~80 lines) builds cleanly through phase9-m1+phase10-hex2; runs and exits 0 with 'not yet implemented' placeholder.  Remaining work for #22/#23/#27 (~2000-3000 lines of M1 across multiple commits, comparable scope to elf64-to-m1.M1): hex2 source tokenizer + width-calc + label/reference tracker, static-label finder (recognizes :ELF_data/:HEX2_data/:GLOBAL_*/:STRING_*/:_string_* prefixes), file mmap helpers (reusable from elf64-to-m1.M1), in-place binary modification (pad, memcpy, pattern-search, RIP-rel32 patch).  Then phase4-amd64-cc-arch.py (#21) and phase1/2-amd64-hex1/hex2.py (#27/#28) patch modes reuse the same patcher.  M2-Planet-compiled C is an option for hex2-data-relocs.py (#23) and m1-to-hex2.py (#24) only — both have callers strictly after phase12 (M2-Planet's phase).  phase5-amd64-m2.py callers include phase5/6/7/8/9 which precede M2-Planet — must be M1.
- 2026-05-24: Task #10 quick check: ran phase39-gnumake against simple/conditional-include/multi-target/.SECONDEXPANSION makefiles directly. All succeed. The known segfault path is in GCC's recursive `make -C gcc` chain (`all-libiberty` exit 139) and the GNU Hello / Automake "alloca.h consideration" segfault from 2026-05-21. Reproducer would need extracting the failing target and running it under phase39; without access to debug phase39's libc (TCC-built, no debugger workflow yet), this stays the documented multi-day blocker codex hit.
- 2026-05-24: Task #8 attempt: flipped phase44 to GCC46_BOOTSTRAP_HOST_CC_SOURCES=0 + GCC46_BOOTSTRAP_HOST_CC_GENERATED=1 (codex's split env from commit 820dba7 already in place). Build progressed past every documented earlier blocker — libcpp, libdecnumber, and the GCC frontend support libs all compiled cleanly through phase37/TCC, no c-lang.o segfault. But the throughput is genuinely ~5 min per source file via the TCC wrapper, and GCC 4.6's frontend has ~250+ translation units, so the realistic wall-clock for one phase44 build is 20+ hours. Codex's session hit the same wall and never converged in finite time. After 8 hours of active cc1 cycling in the kept-failed dir, killed the build and reverted phase44 to HOST_CC_SOURCES=1 (still store-pinned to nixpkgs clang via task #11). Real fix needs phase37 to be sped up (boot3 TinyCC has known slow paths) or replaced before phase44's TCC-only compile is tractable. The phase44 derivation is unchanged from commit e7238cb otherwise.
- 2026-05-23: Task #13 second attempt. Added `GCC_MODERN_BUILD_TARGET_LIBS=1` knob in bootstrap-gcc.sh (appends `all-target-libgcc all-target-libstdc++-v3` to make targets and passes `CFLAGS_FOR_TARGET="-O2 -g0 -isystem $sysroot/include"` + LIBGCC2_INCLUDES + CRTSTUFF_T_CFLAGS so target compiles find <stdio.h>). With the knob set in phase46, phase46 *successfully* builds and packages `$out/include/c++/15.2.0/`, `$out/lib/gcc/.../libgcc.a`, and `$out/lib/libstdc++.a` — the goal of giving phase46 a matching libstdc++ closure works in isolation. BUT phase47 strict's GCC 15.2 self-build then fails at `cfn-operators.pd:192:5 error: no such operator 'BUILT_IN_CBR'`. The new phase46 wrapper picks up its own `include/c++/15.2.0` (correctly matching version) instead of falling back to SDK libc++; somehow the resulting C++ compilation environment changes what build-machine `gencfn-macros` writes into `cfn-operators.pd` (likely float128 enable/disable differs in libstdc++ vs SDK libc++ feature detection), and `genmatch` then can't reconcile that against `match.pd`'s BUILT_IN_CBR family. Reverted the BUILD_TARGET_LIBS=1 from phase46 to keep the chain at `5019a645…` baseline; the knob and the long comment in `phase46-gcc-latest-bootstrap.nix` are kept so the next iteration can either (a) force `--disable-float128` more aggressively in phase47's source-prep, or (b) keep phase47's wrapper falling through to SDK libc++ even when phase46 ships a matching one (defeats the purpose).
- 2026-05-22: Task #13 first attempt blocked. Tried `GCC_MODERN_TARGETS="all-gcc all-target-libgcc all-target-libstdc++-v3"` for phase46 so it ships its own libstdc++ matching GCC 15.2 and the g++ wrapper never falls back to SDK libc++. Target libgcc compile fails at `tsystem.h:95: stdio.h: No such file or directory` because xgcc (the just-built GCC 15.2 frontend) doesn't know the SDK headers — target compiles need `--sysroot` or `-isystem` for libc/SDK headers passed through the libgcc Makefile. Reverted, phase46 stays compiler-only; the SDK path stays on /Library/.../MacOSX.sdk until target libgcc/libstdc++ build is wired.
- 2026-05-22: Modern GCC wrappers (phase45/46/47) no longer reach for `/usr/bin/cc`, `/usr/bin/c++`, or `/usr/bin/ld`. Parameterized the heredocs in `scripts/gcc-modern/bootstrap-gcc.sh` via `wrapper_host_{cc,cxx,ld}` (resolved at heredoc-write time from `GCC_MODERN_HOST_CC` / `GCC_MODERN_HOST_CXX` / `GCC_MODERN_LD` env). Each wrapper also prepends the binutils dir to PATH on entry so a raw nixpkgs clang can posix_spawn ld by name. Phase45/46/47 now export those env vars pointing at `${stdenv.cc.cc}/bin/clang(++)` and `${darwin.binutils-unwrapped}/bin/{as,ld}`, plus `SDKROOT=$GCC_MODERN_SDK_PATH` so clang knows where libSystem lives. `gnu-hello-hash-comparison` reproduces phase46==strict phase47 at new baseline `5019a64510837fae43fc7238b506ec11011542432c792b4ab7683db2e7ff2f73`; previous baseline `b3ec773f...` was Apple-cc-based and no longer reachable from a clean tree.
- 2026-05-21: Strict phase47 failed under the new apple-sdk because the modern g++ wrapper's "SDK libc++" fallback (`$default_sdk/usr/include/c++/v1`) only works against Apple's CLT SDK; nixpkgs `apple-sdk-14.4` does not ship libc++ headers. Tried a `GCC_MODERN_LIBCXX_PATH` env-var fallback pointing at `${llvmPackages.libcxx.dev}`/`${llvmPackages.libcxx}`, but nixpkgs libcxx-21.1.8 expects Clang's `__using_if_exists__` attribute and breaks under GCC 15.2 (`'at_quick_exit' has not been declared`). Reverted phase45/46/47 to CLT SDK; task #11 stays partial (phase44 fully store-pinned). Promoting phase46/47 to also be store-pinned will need phase46 to ship its own matching libstdc++ instead of relying on the SDK libc++ fallback.
- 2026-05-21: Routed phase44 host tooling through Nix store paths. `/usr/bin/as`, `/usr/bin/ld`, `/usr/bin/cc`, and `/Library/.../MacOSX.sdk` are now `${darwin.binutils-unwrapped}/bin/{as,ld}`, `${stdenv.cc.cc}/bin/clang`, and `${apple-sdk}/.../MacOSX.sdk`. nixpkgs clang doesn't auto-find ld or the SDK, so phase44-cxx.sh now prepends `dirname $GCC46_BOOTSTRAP_LD` to PATH and exports `SDKROOT=$PHASE44_SDK_PATH` in macho mode. Phase45/46/47 also use `${apple-sdk}` for `GCC_MODERN_SDK_PATH`. Phase44 + phase46 + phase46 GNU Hello (hash `df4b3ea6c2c1036793ae72e325a618a9e60bd965db8269a2bb4f5b38434226fb`) built; phase47 strict failure surfaced the libc++ gap above.
- 2026-05-21: Audited the current bootstrap fidelity boundary. The realized amd64 chain reaches strict phase47 GCC 15.2.0 and a GNU Hello proof, but it is not yet a faithful hex0-only closure because phase44 still hard-enables host source shortcuts and Apple `/usr/bin` assembler/linker/compiler tools, phase45/46 use modern GCC wrapper shortcuts, phase47 remains compiler-only, and GNU Hello/GCC recipes still depend on nixpkgs `gnumake`.
- 2026-05-21: Tested replacing GNU Hello's bootstrap-package build with `phase39-gnumake`. Keeping phase39 on `PATH` makes configure hang in `make -f -`; invoking phase39 only for the build reaches the generated Automake makefiles and then segfaults immediately while considering `lib/alloca.h`. Reverted GNU Hello to nixpkgs `gnumake` and recorded phase39 as the next concrete make-boundary blocker.
- 2026-05-20: Began repo organization cleanup after matching `gcc_latest` to nixpkgs: refreshed README/current roadmap, split source metadata into `sources.nix`, split GNU Hello proofs into `gnu-hello.nix`, split checks into `checks.nix`, extracted phase34 TinyCC wrapper/header payloads into `scripts/tinycc/` and `bootstrap/headers/`, made the flake default point at strict `gcc_latest`, and removed stale local ignored artifacts.
- 2026-05-20: Rebuilt `phase39-gnumake` and confirmed the old artifact only echoed recipes and failed `-j2` with `creating jobs pipe: error`. The phase now strips the broken broad `return 1` rewrite, uses a smaller fork/exec path, disables GNU make's jobserver pipe path around the current TinyCC/Darwin `pipe(2)` failure, and validates `-j2` recipe-created files in its smoke test. GCC phases still use nixpkgs `gnumake` until this handoff is explicitly tested there.
- 2026-05-19: Formal `gnu-hello-hash-comparison` now builds through phase45 GCC10, phase46 GCC16, strict self-host phase47 GCC16, phase46 GNU Hello, strict phase47 GNU Hello, and the nixpkgs reference. Phase46 and strict phase47 Hello are byte-identical (`4be8eaba032a9e13ea552f8b733f3bac34b7e16287c70db518e06d6f954cd94a`); nixpkgs GCC 15.2.0 Hello differs (`f23f901be1f6c913487bfc939364f746127357805c0ccbe2a922c7c6b793f417`).
- 2026-05-19: Formal strict phase47 now reaches `libcody` with bootstrap C headers filtered, but GCC's builtin include dirs still precede SDK libc++ and make `<cstddef>` include GCC's `stddef.h`; libc++ fallback mode now uses `-nostdinc`, adds libc++ first, then re-adds the GCC internal include/include-fixed dirs and SDK C headers explicitly.
- 2026-05-19: Formal strict phase47 moved from missing GCC16 C++ traits to SDK libc++ compilation, but `libcody` then exposed bootstrap C headers masking SDK C headers; the packaged GCC16 `g++` wrapper now omits the bootstrap sysroot include path in libc++ mode and filters any propagated bootstrap include paths before invoking the real driver.
- 2026-05-19: Formal strict phase47 reached GCC16 build-generator compilation with the phase46 compiler, then failed because the phase46 package only carried the older phase45/GCC4.6 `libstdc++` headers and lacked C++14 traits such as `std::is_trivially_destructible`; the packaged GCC16 `g++` wrapper now falls back to SDK libc++ headers/library when its bundled `include/c++` version does not match the compiler version.
- 2026-05-19: After switching GNU Hello to nixpkgs `gnumake`, configure still selected the GCC driver as `AR` (`cc -c.a`) and the archive step failed on `lib/libhello.a`; the formal Hello proof now exports cctools `ar` and `ranlib` before configure so archive creation stays on the Darwin bintools path.
- 2026-05-19: Formal phase46 GCC16 now builds as `/nix/store/gh08mjr6h92s6fxk1rhxlp14drqv5zds-darwin-minimal-bootstrap-phase46-gcc-16.1.0-amd64`; the next GNU Hello failure was the bootstrap phase39 make hitting `creating jobs pipe: error`, so the formal Hello proof now uses nixpkgs `gnumake` consistently with phases44-47 until phase39 make grows working Darwin job-pipe support.
- 2026-05-19: Formal phase46 now gets past phase45 and through GCC16 `cc1`/`cc1plus`, then fails linking `gcov` because the `g++` wrapper only host-linked frontend binaries while `gcov.o`/`libcommon.a` were compiled by Apple C++ and still carried libc++ `std::__1` symbols; the phase46 host-link shortcut now covers all final `g++` links in `build/gcc` and keeps filtering bootstrap `libstdc++`/`libgcc` flags.
- 2026-05-19: The first host-link shortcut was too broad and regressed phase45, whose frontend objects are still built by the phase44 GCC/libstdc++ path; host frontend links are now controlled by `GCC_MODERN_INPUT_HOST_LINK_SHORTCUTS` and default on only for the `gcc-latest` phase46 shortcut boundary, not for phase45 or strict phase47.
- 2026-05-19: Formal phase46 then built GCC16 frontend objects but failed linking `cc1` because host-shortcut objects carried libc++ `std::__1` symbols while the input wrapper still linked with the phase45 GCC/libstdc++ runtime; the input `g++` wrapper now routes GCC frontend final links (`cc1`, `cc1plus`, `lto1`) through Apple C++ and filters the bootstrap libstdc++/libgcc flags at this host-shortcut boundary.
- 2026-05-19: Formal phase46 passed the GCC16 `genmodes`/`genmddeps` generator boundary, then failed on `genversion.o` because the build-machine flag rewrite had dropped per-object `CFLAGS-$@` defines such as `BASEVER`/`DEVPHASE`; the generator pattern rule now adds `$(CFLAGS-$@)` explicitly while still keeping bootstrap sysroot `CPPFLAGS` off Apple C++.
- 2026-05-19: Formal phase46 then reached GCC 16 build-generator compilation and failed because GCC 16 uses `$(INCINTL)` in `BUILD_CPPFLAGS`, so the prior GCC 10-only `@INCINTL@` rewrite left bootstrap sysroot headers on `/usr/bin/c++` generator compiles; the `Makefile.in` rewrite now handles both forms and strips only the propagated `$(CPPFLAGS)`.
- 2026-05-19: The first MPFR float128 suppression patch regressed formal phase45 because it appended `--disable-float128` inside `extra_mpfr_configure_flags`, turning the generated GMP library path into `.../gmp/ --disable-float128.libs`; the source prep now patches the top-level `Makefile.in` token sequence so `--disable-float128` is a separate configure argument after the GMP path expansion.
- 2026-05-19: Formal phase45 then linked `cc1` and failed because `gimple-match.o` was host-shortcut compiled against libc++ (`std::__1::pair`) while `internal-fn.o` was compiled by phase44 GCC/libstdc++; match-generated sources now stay on the bootstrap compiler path to avoid C++ ABI mixing.
- 2026-05-19: Formal phase46 reached GMP and failed because the GCC 10 wrapper's support-library host shortcut only matched the impure `phase46-gcc-latest/build` path; generalized the shortcut to any Nix or impure `*/build/{gmp,mpfr,mpc,zlib,libiberty,libcpp,libdecnumber,libbacktrace,libcody,gcc,fixincludes}` tree before retrying GNU Hello.
- 2026-05-19: The next formal phase46 retry passed GMP `mpn` with host shortcuts but failed in `mpz/clears.c` because the shortcut still forwarded the bootstrap sysroot `stdarg.h` to Apple clang; the packaged modern wrappers now drop bootstrap sysroot include paths for host-shortcut compiles, matching the earlier input-wrapper generated-source filtering.
- 2026-05-19: Formal phase46 then completed GMP libraries through `scanf`/`rand` and failed only when recursing into the bootstrap-unneeded GMP `demos/calc` yacc path; source prep now removes `tests`, `demos`, `tune`, and `doc` from GMP `SUBDIRS` so phase46 builds the required in-tree GMP libraries without Bison.
- 2026-05-19: Formal phase46 then advanced into MPFR and failed after MPFR autodetected `_Float128` through host probes but Apple clang rejected the resulting declarations during host-shortcut compiles; top-level GCC source prep now appends `--disable-float128` to in-tree MPFR configuration and trims MPFR/MPC recursion to their `src` libraries only.
- 2026-05-19: A first fix for the formal phase45 `stmp-fixinc` blocker tried to run top-level `configure-gcc` before `all-gcc`, but GCC's top-level dependencies pulled bundled GMP configure too early; the fixincludes/no-probe changes now patch `gcc/Makefile.in` before `gcc/` is generated instead.
- 2026-05-19: Formal phase45 reached `cc1`, `cc1plus`, `xgcc`, `cpp`, and GCC driver helper links, then failed because `gcc/Makefile` was generated inside `make all-gcc` after the script's fixincludes patch window.
- 2026-05-19: Formal phase45 then linked up to the first GCC 10 `cc1` and failed on C++-mangled `getrlimit`/`setrlimit`; the staged bootstrap `sys/resource.h` now wraps resource APIs in `extern "C"` for C++ frontend links.
- 2026-05-19: The next formal phase45 retry passed `opts-common.o` and rebuilt through `libcommon-target.a`, then failed at `pretty-print.o` on another GCC 4.6 C++0x narrowing diagnostic; source prep now casts the diagnostic quoting byte explicitly.
- 2026-05-19: Formal phase45 Nix progressed through GCC 10 support libraries, generated-source compilation, ordinary frontend objects, `libbackend.a`, and then failed at `opts-common.o` because GCC 4.6 treats GCC 10's deferred-option aggregate narrowing as an error; the modern GCC source prep now casts that field explicitly instead of broadening global C++ flags.
- 2026-05-19: The first phase45 Nix retry with the formalized phase44 handoff failed because packaged `gcc.real` execs `as` by name and the phase44 output only contained `gcc`/`gcc.real`; phase44 now packages Mach-O `as`, `ld`, and `collect-ld` wrappers in both `$out/bin` and `$out/lib/gcc/x86_64-apple-darwin/4.6.4`.
- 2026-05-19: Rebuilt the formal phase44 derivation after adding packaged tool wrappers; new output is `/nix/store/f73f69s2fagkl4iyfgyyn536npvdgk95-darwin-minimal-bootstrap-phase44-gcc-4.6.4-cxx-amd64` and contains `as`, `ld`, and `collect-ld` in both driver search locations.
- 2026-05-19: The next phase45 Nix configure passed assembler discovery but linked `conftest` with `-syslibroot /`, so `/usr/bin/ld` could not find `libSystem`; the modern GCC script now uses the CLT SDK fallback and phase45/46/47 export that SDK explicitly.
- 2026-05-19: Phase45 then reached `make` but failed on `all-libcody`, which only exists in newer GCC; the modern GCC driver now only prepends `all-libcody` when the generated Makefile defines that target.
- 2026-05-19: Phase45 then hit the current phase39 make pipe/jobserver limitation (`Makefile: pipe: error`) before compiling GCC 10; phase45/46/47 now mirror phase44 by passing nixpkgs `gnumake` through `BOOTSTRAP_MAKE` until phase39 make is promoted.
- 2026-05-19: Phase45 advanced into bundled MPFR and failed compiling `vasprintf.c` because MPFR configure could not find the repaired bootstrap `wchar.h`; modern GCC host library builds now add the build-local bootstrap sysroot include directory through `CPPFLAGS`, `CFLAGS`, and `CXXFLAGS`.
- 2026-05-19: Phase45 then reached GCC configure and failed C++ probes because the universal modern default forced `-std=c++14` on the phase44 GCC 4.6 compiler; the modern driver now probes the input compiler and uses the newest supported C++ mode, falling back to GCC 4.6's `-std=c++0x`.
- 2026-05-19: Phase45 then reached build-machine `libiberty`, where `/usr/bin/cc` picked up bootstrap `stdarg.h` from host `CFLAGS` and failed on the TinyCC-specific `va_arg` macro; the bootstrap sysroot include is now carried in `CPPFLAGS` only so host-library configure/compile probes see it while build-machine tools keep host headers.
- 2026-05-19: The previous phase45 isolation was still incomplete because GCC top-level recursion propagated host `CPPFLAGS` into `build-*` directories; the modern GCC script now patches `BUILD_EXPORTS` and `EXTRA_BUILD_FLAGS` so build-machine subconfigures receive empty `CPPFLAGS_FOR_BUILD` while host libraries still see the bootstrap sysroot.
- 2026-05-19: Phase45 then reached GCC generator compilation and failed because `gcc/Makefile` still built `build/genmodes.o` with bootstrap `CPPFLAGS`; the modern GCC source patch now forces GCC build-generator flags to use `CFLAGS_FOR_BUILD`/`CXXFLAGS_FOR_BUILD` and removes bootstrap `CPPFLAGS` from `BUILD_CPPFLAGS`.
- 2026-05-19: Phase45 generator compilation then exposed GCC 10's Darwin `rlim_t` fallback: `gcc/configure` defined `rlim_t long` after a bootstrap-sysroot probe, conflicting with the real SDK headers used by `/usr/bin/c++`; the source patch now disables that fallback define.
- 2026-05-19: Corrected the `rlim_t` source rewrite after the first attempt matched literal `\n` text instead of configure newlines; the next retry should remove the fallback before `gcc/configure` generates `auto-host.h`.
- 2026-05-19: Phase45 reached GCC build generators, then tried to regenerate `gengtype-lex.c` with missing `flex`; the modern GCC source prep now normalizes GCC generated lexer/parser timestamps so generated C files stay newer than their `.l`/`.y` inputs.
- 2026-05-19: Phase45 then spent minutes compiling generated `insn-recog.c` through the transitional GCC 4.6 C++ frontend; added an env-gated input compiler shortcut for generated GCC sources so Nix formalization can keep moving, while strict phase47 disables it through `GCC_MODERN_WRAPPER_HOST_SHORTCUTS=0`.
- 2026-05-19: The first generated-source shortcut reached `insn-attrtab.c` quickly but leaked the bootstrap sysroot into `/usr/bin/c++`, conflicting with libc++ wrapper headers; host-shortcut compiles now strip the build-local bootstrap sysroot include path.
- 2026-05-19: Switched the phase44 Nix scaffold to pass nixpkgs `gnumake` through `BOOTSTRAP_MAKE` because the current phase39 make artifact can print recipe lines but does not yet execute them; also enabled the previously discovered Mach-O object path, prerequisite archive rebuilds, and host-source shortcut for GCC 4.6 frontend files so Nix matches the stabilized impure phase44 recipe.
- 2026-05-19: Phase44 Nix now reaches Mach-O rebuilds of GCC 4.6 support libraries; fixed stale copied build-dir references and extended the phase37 host overlay `stdint.h` for GMP/MPFR sources that expect C99 least/fast integer typedefs and `SIZE_MAX`.
- 2026-05-19: Phase44 Nix completed the GCC frontend build through `cc1`, `cc1plus`, and `g++`; the direct target runtime step then exposed missing `libgcc.mvars`, `tconfig.h`, and `gsyslimits.h`, so the script now builds `gcc/libgcc-support` after restoring `gsyslimits.h` from the source tree.
- 2026-05-19: The next phase44 Nix retry rebuilt Mach-O GMP/MPFR/MPC and host helper archives, then failed at SDK discovery because `xcrun` is not on the sandbox PATH; Nix now passes `PHASE44_SDK_PATH` and the script has a deterministic CLT SDK fallback.
- 2026-05-19: Phase44 Nix then reached direct target `libgcc` and `libstdc++` configure; libstdc++ failed with `no acceptable ld found in $PATH`, so the phase now installs a build-local `ld` wrapper and exports `AS`/`LD`/`PATH` during direct libstdc++ configure.
- 2026-05-19: Phase44 Nix completed successfully at `/nix/store/4dkz9jc1hqs89dda9i09ggpdnpw7k9r6-darwin-minimal-bootstrap-phase44-gcc-4.6.4-cxx-amd64`, including direct `libgcc`, direct `libstdc++`, package install, and C++ smoke assembly.
- 2026-05-19: GNU Hello Nix formalization initially failed immediately in phase45 because `perl` was not in the builder PATH; added explicit `nativeBuildInputs = [ perl ]` to phase45, phase46, and phase47 modern GCC derivations.
- 2026-05-19: The next phase45 Nix retry failed trying to write `crt_externs.h` into the read-only phase44 store sysroot; `bootstrap-gcc.sh` now copies the selected sysroot to `build/bootstrap-sysroot` and mutates that writable copy.
- 2026-05-19: Extracted remaining embedded phase Python into standalone scripts, wired Nix phase47 strict GCC latest and GNU Hello comparison package entries, and verified no Python heredocs remain under `phases/`, `scripts/`, `packages.nix`, or `flake.nix`.
- 2026-05-19: Rebuilt Nix phase35 GCC 4.6 `all-gcc` after moving source surgery into `scripts/gcc46/phase35-prepare-source.py`; the driver now tolerates the bootstrap option surface and avoids the stale `gccdump.s` dumpbase path.
- 2026-05-19: Fixed the phase36 Nix libgcc boundary by matching the bootstrap libc `struct stat` layout to the generated Darwin headers, staging configure `conftest` sources through the direct `cc1` wrapper, forcing cross mode, and accepting the intentionally empty bootstrap `libgcov.a`.
- 2026-05-18: Repackaged phase45 GCC 10 as a compiler-only handoff with wrapper shortcuts for `conftest` and phase46 support-library compiles; this keeps impure phase46 iteration out of the slow/hanging transitional `cc1` paths.
- 2026-05-18: Reset phase46 after accumulated exploratory `physmem.c` edits, then made the source patch idempotent so Darwin skips non-Darwin libiberty memory headers including `machine/hal_sysinfo.h`.
- 2026-05-18: Fixed the phase45 wrapper's `conftest -E` behavior so phase46 configure detects missing headers such as `process.h` instead of copying raw source and producing false-positive header results.
- 2026-05-18: Killed the slow phase46 `fixincludes` path after it launched long-running GCC10 `cc1` jobs; codified a top-level no-op for fixincludes because the compiler-only modern handoff does not need fixed host headers.
- 2026-05-18: Phase46 completed host support archives through GMP/MPFR/MPC and configured `gcc/`; patched the stale `LIBBACKTRACE` dependency that remained in `gcc/Makefile` even with libbacktrace disabled.
- 2026-05-18: The resumed phase46 frontend then failed on `stmp-fixinc` needing `build-x86_64-apple-darwin/fixincludes/fixinc.sh`; patched `STMP_FIXINC` empty so GCC's internal header staging proceeds without the skipped fixincludes tool.
- 2026-05-18: Stopped another slow phase46 run after GCC10 `cc1plus` spent over a minute each on ordinary frontend sources (`c-lang.cc`, `stub-objc.cc`, `attribs.cc`, `c-errors.cc`); extended the impure wrapper shortcut to phase46 `gcc/` sources so the run can expose structural blockers faster.
- 2026-05-18: Added a disabled-libbacktrace stub archive and stripped remaining stale `LIBBACKTRACE` prerequisites from the compiler-only phase46 GCC makefile path.
- 2026-05-18: Added a phase46 host clang shortcut for GCC frontend sources through the GCC 10 wrapper, including warning-error suppression needed by modern Darwin SDK headers.
- 2026-05-18: Added a phase46 final host C++ link shortcut for GCC build tools while preserving Darwin `-Wl,-syslibroot` flags, avoiding the slow transitional GCC 10 C++ link path during impure discovery.
- 2026-05-18: Completed impure phase46 `all-gcc` and packaged `work/impure/phase46-gcc-latest/out` as a compiler-only handoff with executable `gcc`/`g++` wrappers and a successful smoke compile.
- 2026-05-18: Compared nixpkgs GCC paths: the minimal-bootstrap GCC recipe is reusable as a runCommand-style source/dependency/configure model, and gcc-ng's compiler-only seeding is reusable as a technique; full nixpkgs `stdenv` GCC infrastructure is not usable yet because the phase45/46 compilers are still compiler-only handoffs without complete runtime, wrapper, bintools, and `nix-support` closure.
- 2026-05-17: Resumed impure Mach-O phase44 `all-gcc`; fixed `c-family/c-ppoutput.o` by preserving source subdirectory layout and overlaying sibling `libcpp` headers inside the wrapper temp tree.
- 2026-05-17: Removed phase44's broad source-tree header symlink overlays after they caused `tree.h`/`all-tree.def` to resolve relative to polluted language subdirectories.
- 2026-05-17: Fixed `i386-c.o` and `darwin-c.o` by building real one-level temp overlays for GCC `config` and language directories, including `c-family` and nested `config/.../config` include compatibility.
- 2026-05-17: Fixed `insn-attrtab.o` by staging generated local GCC sources through the temp overlay while preserving the fast no-stage path for plain `conftest.c`.
- 2026-05-18: Stripped debug flags from generated `insn-*.c` bootstrapped compiles and added an impure host-CC escape hatch for those huge generated files; `insn-emit.o` now validates quickly as Mach-O.
- 2026-05-18: Made phase44 pass overridable `PHASE44_CFLAGS`/`PHASE44_CFLAGS_FOR_BUILD`, defaulting to `-g0`; validated `ggc-page.o` through the bootstrapped GCC 4.6 wrapper as Mach-O.
- 2026-05-18: Diagnosed the phase45 GCC 10 driver crash with targeted `xgcc` probes: `-dumpspecs` crashed in diagnostic terminal-width probing, and `-print-sysroot-headers-suffix` crashed through the fatal diagnostic path. The GCC 10 source copy is now patched to avoid both bootstrap-only terminal/error paths.
- 2026-05-18: Rebuilt phase45 `xgcc` impurely after the patches and validated `xgcc -B. -dumpspecs` plus `xgcc -B. -print-sysroot-headers-suffix`; resumed full phase45 `all` without reconfiguring from scratch.
- 2026-05-18: Fixed the next phase45 link blocker by rewriting the bootstrap `sys/times.h` as a C++-safe Darwin declaration; `cc1` no longer carries the mangled `times` reference.
- 2026-05-18: Patched phase45 GCC 10 iteration to no-op bootstrap-only `s-macro_list`, `s-fixinc_list`, and frontend selftests after the transitional `xgcc` hung in preprocessor/self-test probes.
- 2026-05-18: Phase45 reached target `libgcc` configure; pre-seeded known Darwin x86_64 sizeof/decimal/fixed-point answers after autoconf's expected-failure probes produced long-running orphaned `cc1` jobs.
- 2026-05-18: Phase45 then hung on `libgcc`'s `host_address` preprocessor probe. Killed the stuck `xgcc`/`cc1`, patched the source configure script to use the known Darwin x86_64 `host_address=64`, and added `--disable-decimal-float` so future resumes skip the same class of target configure probe.
- 2026-05-18: The first `host_address` rewrite was too literal for GCC 10's generated `libgcc/configure`; replaced it with a bounded multiline rewrite and confirmed the live impure source has no `grep host_address=` probe left.
- 2026-05-18: Phase45 `libgcc` configure next hung in GCC's generic target glibc-version detection on Darwin. Patched GCC/libgcc configure scripts and future configure flags to force the non-glibc `0.0` result.
- 2026-05-18: A resumed phase45 `all-gcc` regenerated `auto-host.h`, reintroducing the modern-Darwin `rlim_t` macro and stale `strsignal` declaration result. The bootstrap script now patches `auto-host.h`, `config.status`, `config.cache`, and `cstamp-h` together before make resumes.
- 2026-05-18: Phase45 `libgcc` configure then reached the remaining target feature probes; pre-seeded AVX/LSE/init-priority answers based on Darwin x86_64 expectations to keep configure from repeatedly compiling probe-only `conftest.c`.
- 2026-05-18: Phase45 target `libgcc` configured and started building, then failed because Darwin's extra parts still requested `libemutls_w.a` while the disabled-shared path had no `emutls_s.o` rule. Removed that bootstrap-unneeded extra part and forced non-emutls TLS detection.
- 2026-05-18: Added `GCC46_BOOTSTRAP_HOST_CC_SOURCES=1` as an impure-only discovery mode after normal GCC frontend files compiled correctly but slowly through the bootstrapped wrapper; `caller-save.o` validates as Mach-O.
- 2026-05-18: The first host-CC discovery run reached `libbackend.a`; added `-Wno-error -Wno-format-security` to the host shortcut and validated the five previously missing backend objects as Mach-O.
- 2026-05-18: Rebuilt phase44 `libcpp`, `cc1`, and `xgcc` after fixing Darwin stdio/stat ABI issues; `xgcc` now emits Mach-O x86_64 objects, and a smoke executable links/runs through the impure Mach-O linker with explicit `-isysroot`, `-nostartfiles`, and `-nodefaultlibs`.
- 2026-05-18: Switched GCC 4.6 cc1 auxbase handling to `%g.o`/`%g.s` because `%|` becomes `-` under `-pipe`; relative auxbase paths were the real Mach-O cc1 crash trigger.
- 2026-05-18: Fixed the Mach-O assembler wrapper to append stdin `-` when `xgcc -pipe` invokes `as` with only options like `-arch x86_64 -o file`.
- 2026-05-18: Forced phase44 target compilation to `-O2 -g0`, disabled libgcc debug info, configured `--disable-threads`, and confirmed the `gthr-single.h` path avoids the missing Darwin `pthread.h` blocker.
- 2026-05-18: Completed direct `make -C build/x86_64-apple-darwin/libgcc all`; disabled bootstrap-unneeded `libgcov` and Darwin/i386 CRT extra-parts to avoid premature runtime/header dependencies.
- 2026-05-18: Confirmed appended top-level no-op stubs do not remove GNU make prerequisites; current fast loop is direct target iteration until the resumed top-level recipe is made dependency-clean.
- 2026-05-18: Directly configured target `libstdc++-v3` with `build/gcc/g++`, bootstrap headers via `-isystem` rather than `--sysroot`, and link probes using `-nostartfiles -nodefaultlibs -Lbuild/gcc -lgcc -Wl,-syslibroot,$SDK -lSystem`; `libsupc++` and `src/.libs/libstdc++.a` now build as Mach-O archives.
- 2026-05-18: Extended the phase34 bootstrap header surface only where proven by the libstdc++ build: C++-safe `NULL`, Darwin/BSD `errno` constants, ctype masks/`__istype`, locale categories, C math declarations, C99 stdint least/fast typedefs, and the missing stdio/string/time/stdlib declarations.
- 2026-05-18: The first C++ link smoke exposed mangled references to C runtime functions (`free`, `malloc`, `abort`, `write`, `memset`, and `fputs`). Fixed the phase34 C headers to wrap their C declarations in `extern "C"` under C++, aligned bootstrap `stdint.h` fast typedefs with GCC 4.6's generated `gstdint.h`, and marked the bootstrap ctype masks as already provided.
- 2026-05-18: Rebuilt phase34 as `/nix/store/sn06mc2kg7wgfpm28gx7kcsjs6kf3mab-darwin-minimal-bootstrap-phase34-tinycc-darwin-cc-amd64`, rebuilt target `libsupc++`/`libstdc++` after removing stale phase37 C header symlinks from `src/gcc`, and verified the installed `libstdc++.a` no longer exports the mangled C runtime references.
- 2026-05-18: Smoke-tested the current phase44 `g++` path end-to-end: `<string>` compiles to a Mach-O x86_64 object, links with the rebuilt static `libstdc++`/`libsupc++`/`libgcc` plus `libSystem`, produces a Mach-O executable, and runs successfully. The remaining link noise is deployment-target warnings, not a correctness blocker.
- 2026-05-18: Folded the discovered direct runtime path into `scripts/gcc46/phase44-cxx.sh`: phase44 now prunes stale phase37 C/POSIX header overlays, defaults top-level work to `all-gcc`, builds target `libgcc` directly, configures/builds/installs target `libstdc++-v3` directly with the phase34 sysroot headers, and manually packages the GCC driver/frontend artifacts instead of invoking GCC's fixincludes-heavy stock install.
- 2026-05-18: Added `PHASE44_SKIP_MAIN_MAKE=1` for fast resumed validation of the codified runtime path. From `work/impure/phase44-gcc46-cxx`, the script now reruns direct target `libgcc`/`libstdc++` packaging without replaying top-level `all-gcc`; validated that installed `out/lib/libstdc++.a` and `out/lib/libsupc++.a` are Mach-O archives and do not contain the previous mangled C runtime references.
- 2026-05-18: Clean scripted phase44 exposed the next seeded-helper boundary: `build/gcov-iov.o` was an ELF phase35 object, so the Darwin link step failed. Added targeted reuse of phase35's Mach-O `build/gcov-iov` and generated `gcov-iov.h` instead of rebuilding that helper during Mach-O iteration.
- 2026-05-18: The next clean phase44 retry moved past `gcov-iov`; frontend host-shortcut compiles then picked phase35-copied bootstrap `stdarg.h` symlinks from prerequisite directories, which only the TinyCC-derived wrapper understands. The script now prunes stale phase34 header symlinks from copied prerequisite trees so Darwin host compiles use system/compiler headers.
- 2026-05-18: Resume validation showed GNU make still preserves old top-level `all-gcc` prerequisites even after appending no-op recipes, causing seeded GMP/libiberty to rebuild. Phase44 now configures `gcc/` when needed and runs `make -C gcc all` directly for the default GCC frontend build.
- 2026-05-18: Host-shortcut GCC frontend compiles then hit Darwin SDK compatibility issues: fortify rewrote GCC 4.6's fallback `snprintf`/`vsnprintf` declarations, `auto-host.h` redefined Darwin `rlim_t`, and `strsignal`'s constness probe was wrong for modern SDK headers. Phase37's impure Mach-O host shortcut now overlays minimal host-only headers to neutralize those SDK/GCC 4.6 conflicts.
- 2026-05-18: The next frontend file, `dfp.c`, exposed a second host-only SDK conflict: libdecnumber's generated `gstdint.h` intentionally defines GCC 4.6 fast typedefs that conflict with modern Darwin `stdint.h`. The phase37 host overlay now supplies exact-width Darwin integer typedefs while suppressing the system fast typedef block.
- 2026-05-18: Frontend `make -C gcc all` then reached bootstrap-unneeded helper links (`gcov`, `gcov-dump`) and a missing generated i386 include. Phase44 now reuses the phase35 Mach-O helper binaries and seeds `i386-builtin-types.inc` along with the other generated GCC files.
- 2026-05-18: Tightened the direct GCC frontend build to the artifacts needed by the bootstrap handoff (`xgcc`, `cc1`, and the C++ frontend target) instead of GCC's full `all`, which pulls in non-essential `gcov` helper links against still-seeded prerequisite archives.
- 2026-05-18: Rebuilding Mach-O `libiberty.a` exposed another modern Darwin header mismatch: `strsignal.c` tried to provide its own `psignal` despite the SDK declaration taking `const char *`. The host overlay now marks `psignal` as provided for these impure Mach-O archive rebuilds.

## Current runnable chain

- [x] Standalone `nixpkgs-darwin-bootstrap` repo with flake outputs.
- [x] amd64 Darwin `hex1` from stage0 `AMD64/hex1_AMD64.hex0`.
- [x] amd64 Darwin `hex2-0` from stage0 `AMD64/hex2_AMD64.hex1`.
- [x] amd64 Darwin `catm` from stage0 `AMD64/catm_AMD64.hex2`.
- [x] amd64 Darwin `M0` from stage0 `AMD64/M0_AMD64.hex2`.
- [x] amd64 Darwin `cc_arch` assembles and signs as a Mach-O candidate.

## Immediate blocker

- [x] Fix `M0-darwin` output fidelity for strings and raw string tokens.
- [x] Fix `cc_arch-darwin` runtime fidelity for M2-Planet input.
  - Static cc_arch data is copied into `__DATA` and RIP-relative references are redirected there.
  - cc_arch heap allocation starts after the copied static region instead of overwriting it.
  - The concatenated Darwin `M2-0.c` compiles to `M2-0.M1`.

## Next chain tasks

- [x] Package amd64 Darwin `cc_arch`.
  - Build `cc_arch-0.hex2` with `M0-darwin`.
  - Concatenate Mach-O low-data header and `cc_arch-0.hex2` with `catm-darwin`.
  - Assemble with `hex2-darwin`, pad to `__LINKEDIT`, sign, and smoke-test.
- [x] Package amd64 Darwin `M2` candidate.
  - Concatenate Darwin M2libc bootstrap C and M2-Planet C sources with `catm-darwin`.
  - Compile to M1 with `cc_arch-darwin`.
  - Prefix Darwin M2libc defs/core, assemble with `M0-darwin`, link with `hex2-darwin`, relocate static data into `__DATA`, sign, and smoke-test startup.
- [x] Fix `M2-darwin` option/file runtime.
  - Preserve Darwin `envp` in the startup frame so generated `main` locals land at the offsets M2 expects.
  - Encode `FILE*` descriptors as `fd + 1` so fd 0 is not confused with `NULL`.
  - `M2-darwin --help` and `M2-darwin -f trivial.c -o trivial.M1` now work.
- [x] Build MesCC tools under the Darwin chain.
  - [x] Build `blood-macho-0` from `M2-darwin` and smoke-test footer generation.
  - [x] Build `M1-0` from `M2-darwin` and smoke-test M1 to hex2 conversion.
  - [x] Add amd64 Darwin full M2libc syscall/core layer for MesCC tools.
  - [x] Build `hex2-1` from `M2-darwin` and `M1-0`.
  - [x] Build full `M1` from `M2-darwin`, `M1-0`, and `hex2-1`.
  - [x] Build full `hex2` from `M2-darwin`, full `M1`, and `hex2-1`.
  - [x] Build `kaem` from `M2-darwin`, full `M1`, and full `hex2`.
  - [x] Build full `M2-Planet` from the Darwin MesCC toolchain.
- [ ] Bootstrap GNU Mes Scheme from Darwin MesCC/M2 outputs.
  - [x] Confirm the required ordering from Nixpkgs minimal-bootstrap.
    - Linux minimal-bootstrap builds `mes-m2`, Mes libraries, and the Mes Scheme interpreter before TinyCC.
    - TinyCC is compiled by MesCC through `mes --no-auto-compile -e main mescc.scm --`, not directly by M2-Planet.
  - [ ] Add a Darwin Mes source-prep phase.
    - [x] Start from GNU Mes `0.27.1`, matching Nixpkgs minimal-bootstrap.
    - [x] Generate `include/mes/config.h` for amd64 Darwin sizes and Mes version.
    - Replace Linux include/module assumptions with Darwin paths before running Mes kaem scripts.
  - [ ] Port Mes libc and MesCC support to Darwin.
    - Add Darwin syscall numbers, `crt1`, `setjmp`/`longjmp`, `kernel-stat`, signal, and file API shims needed by Mes.
    - Produce Darwin `libc-mini`, `libmescc`, `libc`, and `libc+tcc` archives from the existing signed `M1`/`hex2` chain.
    - [x] Build `libc-mini.M1` with Darwin MesCC syscall shims and run a signed `puts` smoke binary.
    - [x] Build `libmescc.M1` with Darwin `syscall-internal.c` and verify `__raise` resolves.
      - [x] Add and pass `phase20-mescc-libmescc-probe`.
    - [x] Build broad `libc.M1` by replacing Linux syscall translation units with Darwin shims.
      - [x] Add and pass `phase21-mescc-libc-probe`.
    - [x] Build `libc+tcc.M1` and add/stub the extra file APIs needed by bootstrappable TCC.
      - [x] Add and pass `phase22-mescc-libc-tcc-probe`.
    - [ ] Re-link Mes itself as a signed Mach-O using MesCC-generated Mes objects plus Darwin libc.
  - [ ] Build and sign `mes-m2`.
    - [x] Probe the Mes bootstrap script through its initial `M2-Planet` compile to `mes.M1`.
    - [x] Probe the post-M2 Mach-O link path through Darwin `M1` and `hex2`.
      - The signed Mach-O `mes-m2` now boots Mes Scheme with `MES_PREFIX`/`GUILE_LOAD_PATH` and evaluates a smoke-test expression.
    - [x] Package `mes-m2` with a substituted Darwin `mescc.scm` and NYACC load path.
      - `mes-m2` now drives `mescc.scm -S` far enough to compile a trivial C translation unit to M1.
    - [x] Link and run a signed Mach-O from MesCC-generated M1.
      - A MesCC-compiled trivial C `main` links with a Darwin MesCC `crt1.M1`, signs, and exits cleanly.
    - Run the Mes bootstrap kaem script through `phase11-kaem`.
    - Link with the Mach-O template, pad `__LINKEDIT`, sign, and smoke-test `mes-m2 --version`.
  - [ ] Rebuild full Mes Scheme on Darwin.
    - Compile Mes sources using `mes-m2` plus Darwin Mes libraries.
    - Install `mes`, `mescc.scm`, Mes modules, headers, and libraries as a Darwin bootstrap output.
    - Smoke-test `mes --version` and a tiny `mescc.scm -S` compile.
- [ ] Bootstrap TCC from Darwin Mes Scheme/MesCC outputs.
  - [x] Identify the smallest bootstrappable TCC fork inputs.
    - Nixpkgs uses Jan Nieuwenhuizen's `tinycc` fork at `ea3900f6d5e71776c5cfabcabee317652e3a19ee` for the MesCC-oriented TCC seed.
  - [x] Verify that the fork is not directly M2-Planet-compilable.
    - The probe is a negative check only: M2-Planet reaches non-M2 C constructs in the vendored fork before code generation.
  - [x] Vendor the bootstrappable TinyCC fork in `vendor/tinycc-bootstrappable`.
  - [x] Add a reproducible M2-Planet probe for the vendored fork.
    - Current pristine-fork probe reaches `elf.h:88`; the failure confirms this is not a direct M2-Planet input.
  - [x] Compile `tinycc-boot-mes` with Darwin MesCC.
    - Use Mes `libc+tcc`, Darwin include paths, and `CONFIG_TCCBOOT`/`TCC_MES_LIBC`.
    - Replace Linux ELF interpreter/library paths with Darwin Mach-O/linker settings.
    - Link, pad, sign, and smoke-test `tcc -version`.
    - [x] First emit `tinycc-boot-mes.M1` with `mes-m2 --no-auto-compile -e main mescc.scm -- -S`.
      - [x] Add and pass `phase19-tinycc-mescc-m1-probe` using a derived MesCC-patched TinyCC source.
      - [x] Increase the Darwin Mes heap and run MesCC with the same large stack/arena sizing used by the MesCC wrapper path.
      - [x] Rewrite Mes' M2-built assertion reporter to avoid eager `&&` dereferences so bootstrap failures print useful diagnostics.
      - [ ] Investigate remaining MesCC type warnings emitted while producing `tcc.M1`.
    - [x] Link `tinycc-boot-mes.M1` with Darwin `libc+tcc.M1` into a signed Mach-O.
      - [x] Build a Darwin `libmescc.M1` archive checkpoint.
      - [x] Build a broad Darwin `libc.M1` archive checkpoint.
        - [x] Add and pass `phase21-mescc-libc-probe`.
      - [x] Build a Darwin `libc+tcc.M1` archive checkpoint.
        - [x] Add and pass `phase22-mescc-libc-tcc-probe`.
      - [x] Add `phase23-tinycc-mescc-link-probe` to link, pad, sign, and run `tcc -version`.
      - [x] Preserve Darwin `argc`/`argv`/`envp` on the MesCC stack frame through `crt1-libc.M1`.
      - [x] Patch phase5 static-data relocation for RIP-relative `%rdi`/`%rsi` forms used by TinyCC globals.
      - [x] Remove the temporary early `tcc.c` version-return shortcut; `tcc -version` now runs through `tcc_new` and argument parsing.
    - [x] Patch or gate TinyCC's ELF-only paths until `tcc -version` runs before enabling self-hosting.
  - [ ] Run TinyCC self-advance stages.
    - [x] Add a `phase24` smoke stage that exercises real preprocessing and object generation.
      - MesCC-built TinyCC now preprocesses a C file and emits an x86_64 ELF relocatable object.
    - [x] Debug TinyCC self-compilation with Mes includes before enabling boot0.
      - Avoid the copied-`__DATA` function-pointer trap by replacing switch-case `qsort` with a direct insertion sort in the TinyCC Mes patch.
      - Add `phase25-tinycc-self-object-probe` to compile TinyCC itself to an x86_64 ELF relocatable object.
    - [x] Add a narrow ELF relocatable to signed Mach-O checkpoint for TinyCC output.
      - `phase27-tinycc-elf-to-macho-probe` compiles a C object with the MesCC-built TinyCC, converts the ELF relocatable to M1, links a signed Mach-O, and verifies exit status 42.
    - [x] Convert the full self-compiled TinyCC ELF relocatable to M1.
      - `phase28-tinycc-self-m1-probe` translates `tcc.o` into M1/hex2 while preserving unresolved SysV libc references for the next link stage.
    - [x] Prove multi-object SysV ABI linking for TinyCC output.
      - `phase29-tinycc-sysv-libc-probe` compiles both a caller and a seed `strlen` implementation with bootstrapped TinyCC, converts both ELF relocatables to M1, links a signed Mach-O, and verifies exit status 9.
    - [x] Link the self-compiled TinyCC object with a seed SysV Darwin libc candidate.
      - `phase30-tinycc-self-link-candidate` produces a signed `tcc-self-candidate`; source-guided data relocation and SysV stack alignment make `tcc-self-candidate -version` run successfully.
    - [x] Prove the self-host candidate can compile and link a C smoke object.
      - `phase31-tinycc-self-compile-probe` compiles `hello.c` with `tcc-self-candidate`, converts the resulting ELF relocatable to M1, links a signed Mach-O, and verifies exit status 42.
    - [x] Compile TinyCC again with the self-host candidate.
      - `phase32-tinycc-boot1-object-probe` uses `tcc-self-candidate` to compile the patched TinyCC source into a new x86_64 ELF relocatable `tcc-boot1.o`.
    - [x] Link a TinyCC boot1 binary candidate.
      - `phase33-tinycc-boot1-link-candidate` links `tcc-boot1.o` into a signed Mach-O; the candidate runs but does not yet pass `-version`, so `phase30` remains the current usable self-hosted TinyCC.
    - [x] Add a TinyCC-backed Darwin `cc` wrapper.
      - `phase34-tinycc-darwin-cc` wraps `tcc-self-candidate`, the ELF-to-M1 bridge, the seed SysV libc, and Mach-O signing to build runnable Darwin executables from simple C inputs.
      - The wrapper now supports `-E`, materializes quote-include headers for GCC-style build directories, and carries a minimal bootstrap header set.
    - [ ] Add the ELF/Mach-O link boundary needed to turn the self-compiled TinyCC object into a runnable compiler.
    - Build `tinycc-boot0`, `tinycc-boot1`, `tinycc-boot2`, `tinycc-boot3`, and final `tinycc-bootstrappable`.
    - Rebuild `libtcc1.a` at each required feature level.
  - [ ] Port TinyCC output to signed Mach-O/Darwin.
    - Replace ELF object/executable emission and Linux runtime assumptions.
    - Build and run a signed Mach-O TCC that compiles a hello-world Mach-O.
- [ ] Bootstrap toward GCC 4.6 on Darwin.
  - [ ] Establish the TCC-hosted C toolchain boundary: preprocessor, assembler input, object output, and runnable host tools.
  - [x] Add a `phase26-gcc46-source` source checkpoint with GCC 4.6.4 plus GMP 4.3.2, MPFR 2.4.2, and MPC 0.8.1.
  - [x] Add a patched GCC 4.6 Darwin bootstrap source.
    - `gcc46-darwin-bootstrap-src` applies `patches/gcc46-darwin-bootstrap.patch`, currently stubbing libiberty regex and legacy C++ demangling paths that the MesCC-built TinyCC cannot compile yet.
  - [ ] Advance `make all-gcc` past libiberty.
    - Current boundary: GCC and libiberty configure succeed for `x86_64-apple-darwin`; libiberty now compiles through `lrealpath.c` and stops while compiling `make-relative-prefix.c`.
  - [ ] Add bootstrap prerequisites in order: binutils/cctools-equivalent assembler+linker path, make/shell assumptions, GMP/MPFR/MPC as required.
  - [ ] Build GCC 4.6 stage1 with the bootstrapped C compiler, then iterate to a self-hosted stage2/stage3.
  - [ ] Keep each successful boundary as a Nix phase and a git checkpoint.

## aarch64 follow-up

- [ ] Revisit aarch64 after the amd64 Darwin chain is stable.
  - Replace the low-address ELF-era writable data assumptions.
  - Use high-base Mach-O templates and Darwin `LC_MAIN` argv throughout.

## Running log

- 2026-05-08: Switched `todos.md` progress tracking to append-only log entries; keep the checklist as a roadmap and record each new bootstrap boundary here instead of rewriting prior TODO text.
- 2026-05-08: Added proper GCC 4.6 bootstrap patch hunks for libiberty `fnmatch.c`, `getopt.c`, `hex.c`, and `make-relative-prefix.c`; latest run reached GCC subconfigure after passing the prior libiberty compile boundary.
- 2026-05-08: Validated the `make-relative-prefix.c` bootstrap stub in `make all-gcc`; libiberty now compiles through `make-relative-prefix.c` and stops while compiling `make-temp-file.c`.
- 2026-05-08: Added a bootstrap `make-temp-file.c` implementation; libiberty now compiles through `objalloc.c` and stops while compiling `obstack.c`.
- 2026-05-08: Added a minimal bootstrap `obstack.c`; libiberty now compiles through `physmem.c` and stops while compiling `pex-common.c`.
- 2026-05-08: Added minimal `pex-common.c` process API stubs; libiberty now compiles through `pex-one.c` and stops while compiling `pex-unix.c`.
- 2026-05-08: Added a minimal `pex-unix.c` bootstrap stub; libiberty now compiles through `pex-unix.c` and stops while compiling `safe-ctype.c`.
- 2026-05-08: Added `EOF` to the TinyCC Darwin bootstrap `stdio.h`; GCC and libiberty configure now complete, libiberty compiles through `safe-ctype.c`, and the next boundary is a TinyCC crash while compiling `simple-object-mach-o.c`.
- 2026-05-08: Added a minimal `simple-object-mach-o.c` bootstrap stub after the TinyCC crash; libiberty now compiles through `simple-object-mach-o.c` and stops while compiling `strsignal.c`.
- 2026-05-08: Added a minimal `strsignal.c` bootstrap stub; libiberty now compiles through `strsignal.c` and stops while compiling `bsearch.c`.
- 2026-05-08: Added a simple linear `bsearch.c` bootstrap implementation; libiberty now compiles through `mempcpy.c` and stops while compiling `mkstemps.c`.
- 2026-05-08: Added a minimal `mkstemps.c` bootstrap stub; libiberty now compiles through `mkstemps.c` and stops while compiling `putenv.c`.
- 2026-05-08: Added a minimal `putenv.c` bootstrap stub; libiberty now compiles through `random.c` and stops while compiling `rename.c`.
- 2026-05-08: Added a minimal `rename.c` bootstrap stub; libiberty now compiles through `rindex.c` and stops while compiling `setenv.c`.
- 2026-05-08: Added minimal `setenv.c`/`unsetenv.c` bootstrap stubs; libiberty now compiles through `setenv.c` and stops while compiling `sigsetmask.c`.
- 2026-05-08: Added a minimal `sigsetmask.c` bootstrap stub; libiberty now compiles through `strverscmp.c` and stops while compiling `tmpnam.c`.
- 2026-05-08: Added a minimal `tmpnam.c` bootstrap stub; host libiberty now archives successfully and `make all-gcc` advances to GMP, stopping while compiling `gen-fac_ui.c`.
- 2026-05-08: Added TinyCC-safe GMP bootstrap generators for factorial, Fibonacci, base, and perfect-square tables; GMP now generates its derived files and stops while compiling `mpn/add.c`.
- 2026-05-08: Added archive/library expansion to the TinyCC Darwin `cc` wrapper and replaced GMP's build with a minimal bootstrap `libgmp.a`; `make all-gcc` now reaches MPFR and stops while compiling `mpfr/get_d.c`.
- 2026-05-08: Replaced MPFR's build with a minimal bootstrap `libmpfr.a`; `make all-gcc` now builds GMP, MPFR, MPC, host libiberty, and stops in build-side `fixincludes/fixincl.c`.
- 2026-05-08: Skipped the build-side `fixincludes` executable in the bootstrap source; `make all-gcc` now advances through fixincludes and stops at a TinyCC crash while compiling `zlib/gzio.c`.
- 2026-05-08: Replaced zlib's build with a minimal bootstrap `libz.a` and allowed libcpp's no-dependency mode; `make all-gcc` now advances through zlib and stops at a TinyCC parse failure while compiling `libcpp/charset.c`.
- 2026-05-08: Added libcpp configure fallbacks for host word sizes and replaced `charset.o` with a TinyCC-safe bootstrap implementation; `make all-gcc` now compiles `charset.o` and stops while compiling `libcpp/directives.c`.
- 2026-05-08: Replaced libcpp's archive with a minimal bootstrap `libcpp.a` and fixed its no-dependency object rule to preserve `bootstrap-libcpp.o`; `make all-gcc` now advances through libcpp and stops in `libdecnumber` configure while detecting fixed-width integer types.
- 2026-05-08: Added libdecnumber fixed-width type cache fallbacks and replaced its archive with a minimal bootstrap `libdecnumber.a`; `make all-gcc` now advances into the GCC build directory and stops compiling `build/genconstants.o`.
- 2026-05-08: Expanded the phase34 bootstrap libc and GCC configure size fallbacks; `make all-gcc` now compiles and links `build/genconstants`/`build/genmddeps`, runs `genconstants`, and stops when `build/genmddeps` bus-errors while generating `tmp-mddeps`.
- 2026-05-08: Fixed TinyCC ELF-to-M1 local-label/data absolute relocations and stubbed `genmddeps`; phase34 now validates static data and duplicate string relocations, and `make all-gcc` advances past `s-mddeps` to a `build/genconstants` bus error while generating `tmp-constants.h`.
- 2026-05-08: Added a TinyCC-safe AWK path for `insn-constants.h` generation and filtered `s-constants` out of the generic `build/gen%` rule; the patched GCC bootstrap source derivation now applies cleanly, with full `make all-gcc` validation running in `/var/folders/lj/fzbygbp11n3g64qh3ntwqtc80000gn/T/tmp.cAiPUDXQNE`.
- 2026-05-09: Bypassed GCC `genmodes` for the bootstrap path with checked-in x86_64 Darwin mode outputs after TinyCC-built `genmodes` exposed string/data relocation corruption; `make all-gcc` now advances past `s-modes-h` and stops compiling build-side `genflags.o` on another included-`.def` preprocessor backslash failure.
- 2026-05-09: Inlined the remaining GCC build-generator `.def` inclusions needed for TinyCC preprocessing (`darwin.h`, `rtl.h`, and `gengenrtl.c`); `make all-gcc` now builds and runs `gengenrtl`, generates `genrtl.h`, and stops at a TinyCC crash while compiling build-side `gengtype.o`.
- 2026-05-09: Replaced GCC `gengtype` execution with bootstrap stubs for `gtype-desc.[ch]` and `gt-*.h` headers to avoid the TinyCC crash in the build generator; `make all-gcc` now generates the GC header set and stops compiling build-side `rtl.o` on another `rtl.def` include expansion failure.
- 2026-05-09: Added generated GCC `rtl.c` bootstrap include tables so TinyCC no longer preprocesses `rtl.def` inside runtime RTL arrays; `make all-gcc` now compiles `build/rtl.o`, `build/read-rtl.o`, and `build/ggc-none.o`, then stops on a TinyCC preprocessor backslash failure while compiling build-side `vec.o`.
- 2026-05-09: Made GCC build-side `vec.c` avoid the large `vec.h` macro layer under `GENERATOR_FILE` and replaced the stack-vector bookkeeping with a small direct array; `make all-gcc` now compiles the build RTL support objects through `print-rtl.o` and stops linking `build/genflags` on a TinyCC/M1 target-label relocation failure for `ggc_alloc_zone_vec_rtvec_def`.
- 2026-05-09: Added bootstrap definitions for `ggc_alloc_zone_vec_rtvec_def` and the public `obstack_free` wrapper so build-side GCC generators link further; `make all-gcc` now gets past the prior `build/genflags` missing-label failures and stops on the next missing libc helper, `atol`.
- 2026-05-09: Added bootstrap `atol` and a generator-side `vec_heap_free` fallback after `build/genflags` exposed missing runtime labels; `make all-gcc` now links and runs `genflags`, then stops when TinyCC-built `genconditions` bus-errors while generating `tmp-condmd.c`.
- 2026-05-09: Stubbed the GCC condition, flags, tree-check, predicate, and constraint generator outputs that were tripping TinyCC/M1 generator runtimes; `make all-gcc` now gets through those bootstrap prerequisites and stops when `genattr` bus-errors while producing `insn-attr.h`.
- 2026-05-09: Added a `bootstrap-md-stubs.sh` path for the remaining GCC MD-generated headers and sources so the Darwin bootstrap no longer runs the fragile TinyCC-built `genattr`/`gencodes`/RTL C generators; the patched GCC source derivation applies cleanly and the next validation step is a fresh `gcc46-darwin-bootstrap` build with the patched Makefile rules.
- 2026-05-09: Added `phase35-gcc46-all-gcc` as the fresh Nix driver for GCC 4.6 `make all-gcc`, wired it to the phase34 TinyCC Darwin wrapper, seeded GCC/libiberty configure caches, and exported the phase from the flake.
- 2026-05-09: Made the phase34 `tcc-darwin-cc` wrapper answer `--version`, added cctools `ar`/`nm`/`ranlib`/`strip`/`lipo`/`otool` paths for the sandboxed GCC build, stubbed `gcov-iov.h` generation, and advanced the Nix build through host libiberty and GMP to the MPFR GMP-link probe.
- 2026-05-09: Bypassed MPC's non-cacheable MPFR link probe and replaced MPC with a minimal bootstrap `libmpc.a`; `phase35-gcc46-all-gcc` now builds through GMP, MPFR, MPC, and advances into the later target-side libiberty build before the 30-minute validation timeout.
- 2026-05-09: Fixed phase34 archive expansion to use the vendored cctools `ar`, trimmed the GCC phase35 target to the current bootstrap `xgcc` milestone, and installed `xgcc` as a TinyCC-backed driver shim; `phase35-gcc46-all-gcc` now builds successfully and validates the installed `xgcc` by compiling/running a Darwin smoke binary that exits 42.
- 2026-05-09: Added `phase36-gcc46-cc1` as the next real GCC 4.6 frontend build attempt on top of the phase35 `xgcc` milestone, preserved the phase35 build/source work area for follow-on phases, repaired relocated GCC Makefile paths and copied source header/definition trees needed by TinyCC, and replaced the first two fragile C frontend translation units (`c-lang.c`, `c-family/stub-objc.c`) with bootstrap-safe patch hunks. `phase35-gcc46-all-gcc` still validates with the Darwin smoke program exiting 42; `phase36-gcc46-cc1` now gets past `c-lang.o` and `stub-objc.o` and stops at `attribs.o`, where the MesCC-built TinyCC frontend corrupts preprocessing/parsing after a large GCC header expansion (`will touch memory past end of the struct` followed by a malformed token diagnostic).
- 2026-05-10: Reverted the GCC 4.6 semantic stub patch series and removed the remaining c-lang/stub-objc bootstrap replacements from the shared Darwin bootstrap patch. The next phase is to make the stage compiler compile real GCC sources instead of replacing GCC translation units with stubs.
- 2026-05-10: Fixed the phase34 Darwin TinyCC syscall bridge to translate carry-flag syscall failures into negative errno returns. This removes the false-success include-open corruption that made missing include-path candidates feed garbage into the preprocessor; a GCC `c-lang.c` preprocessing repro now completes cleanly with real unstubbed GCC sources.
- 2026-05-10: Added the missing Darwin bootstrap `ino_t` and `dev_t` typedefs used by libcpp's `struct cpp_dir`; with regenerated phase34 headers, TinyCC compiles the real GCC `c-lang.c` translation unit to an object instead of stopping at the prior `cpplib.h` parser diagnostic.
- 2026-05-10: Used LLDB on a re-signed copy of the MesCC-built TinyCC and traced the `c-decl.c` crash into bootstrap libc varargs formatting (`append_str` via `tcc_warning`). Replaced the x86_64 bootstrap `stdarg.h`/`__va_start`/`__va_arg` path with the TinyCC-compatible register-save implementation and fixed the inline-function filename allocation; the `c-decl.i` repro now compiles to an object with valid diagnostics instead of crashing.
- 2026-05-13: Restarted phase36 without the earlier 7200s timeout after the old session disappeared with no valid output. Phase36 now gets past `c-decl.o`, `c-parser.o`, and the first generated programs, then fails linking/running `build/genhooks` because the bootstrap libc lacks `fscanf` and `strpbrk`. Added minimal implementations and prototypes for the exact generator patterns (`%*[^@]`, `%N[^...]`, `%Ns`) so this stays a libc/bootstrap fix rather than a GCC source stub.
- 2026-05-13: Rebuilt phase36 with the `fscanf`/`strpbrk` fix; it now links and runs `build/genhooks`, generates `target-hooks-def.h`, and advances through more C-family objects before stopping at `c-family/c-pch.o`. Root cause is the bootstrap `stdio.h` lacking `BUFSIZ`, which makes GCC `output.h` skip `extern FILE *asm_out_file`; added `BUFSIZ` to the bootstrap header.
- 2026-05-13: Rebuilt phase36 with `BUFSIZ`; it now compiles through `c-pch.o`, generated insn stubs, `ggc-page.o`, and stops in `alias.o` because the bootstrap `gengtype` replacement emits empty GC headers. Added fallback GC allocator declarations/definitions for the C frontend allocator names and the scalar splay-tree allocator callbacks.
- 2026-05-13: Rebuilt phase36 with gtype allocator fallbacks and hit a regression in `c-decl.c`: ordinary fallback allocators were declared with `(void)`, but real GCC allocator call sites pass allocation sizes. Switched those fallback declarations/definitions to old-style unspecified arguments while keeping the splay-tree callback allocators explicitly typed.
- 2026-05-13: Rebuilt phase36 after loosening gtype allocator fallbacks; it now passes `c-decl.o`, `alias.o`, the MD source stubs, `builtins.o`, and `caller-save.o`, then stops at `calls.o` because the bootstrap `s-flags` rule emits an empty `insn-flags.h`. That leaves `GCC_INSN_FLAGS_H` undefined, so `defaults.h` skips stack-check defaults such as `STACK_CHECK_MAX_VAR_SIZE`; changed the stub header to provide the normal include guard.
- 2026-05-13: Rebuilt phase36 with the guarded `insn-flags.h` stub; it now passes `calls.o`, `cfg*.o`, `combine*.o`, and `df-*.o`, then stops at `dfp.o` because libdecnumber's generated `gstdint.h` redefines `uintptr_t` despite the bootstrap `stddef.h` already providing it. Forced the libdecnumber stdint fallback to record `uintptr_t` as present when it is using `stddef.h`.
- 2026-05-13: Rebuilt phase36 with the libdecnumber `uintptr_t` fix; it now passes `dfp.o`, `diagnostic.o`, `dojump.o`, and several Dwarf-adjacent objects, then stops at `dwarf2asm.o` because the bootstrap `gengtype` replacement lacks the string/tree-node and tree-node/tree-node splay allocator callbacks and still omits several C/Dwarf GC allocator fallbacks. Expanded the generated `gtype-desc` stub with those callbacks and the C/Dwarf allocator names already reached by this build.
- 2026-05-13: Rebuilt phase36 with expanded gtype allocator stubs; it now passes `dwarf2asm.o`, `dwarf2out.o`, `emit-rtl.o`, `ira*.o`, the LTO streamer objects, and stops at `mode-switching.o` because the attr stub lacks GCC's generated `i387_cw` enum (`I387_CW_ANY`, `I387_CW_UNINITIALIZED`, etc.). Added the i386 `i387_cw` enum/prototype/default getter to the attr stub and recorded the additional GC allocator names that compiled with implicit declarations in this pass.
- 2026-05-13: Rebuilt phase36 with the `i387_cw` attr enum; it gets through `mode-switching.o` and reaches `insn-attrtab.o`, where the attrtab source stub itself fails because it returns `I387_CW_ANY` without including the generated `insn-attr.h`. Included that header in the attrtab stub and switched generated cleared GC allocator fallbacks to the existing `ggc_alloc_cleared_atomic` macro to avoid implicit-int pointer truncation in `gtype-desc.c`.
- 2026-05-13: Rebuilt phase36 with the attrtab include fix; it now compiles through `mode-switching.o`, LTO streamer objects, and stops at `optabs.o` because the static bootstrap `insn-codes.h` only declares `CODE_FOR_nothing`. Reworked the MD stub generator to emit target/source-referenced `CODE_FOR_*` enum values, generate matching no-op `gen_*` machine-pattern stubs, and define a sized bootstrap `insn_data` table for the later link step.
- 2026-05-13: Pivoted away from the fake GCC 4.6 `cc1` loop. Removed the shared `gcc46-darwin-bootstrap.patch` application and deleted the accumulated GCC source patch file so the GCC source checkpoint is clean again; the next GCC attempt should happen only after the TinyCC self-host path and prerequisite bootstrap tools are real enough to run GCC's own generators instead of stubbing them.
- 2026-05-13: Rechecked the earlier TinyCC self-host blocker after the libc/relocation fixes: `phase33-tinycc-boot1-link-candidate` now runs `tcc-boot1 -version` successfully and compiles a C file to an ELF relocatable. Promoted phase33 from expected-failure probe to validated boot1 candidate and switched the Darwin `tcc-darwin-cc` wrapper to use `tcc-boot1-candidate` instead of the phase30 self-candidate.
- 2026-05-13: Added explicit TinyCC self-advance phases after the promoted boot1 candidate: boot2 object/link and boot3 object/link derivations, each compiling TinyCC with the previous candidate, linking a signed Mach-O, checking `-version`, and proving it can compile an ELF relocatable. The `tcc-darwin-cc` wrapper now uses the boot3 candidate as its compiler input.
- 2026-05-13: Started the prerequisite tool stack by adding a `phase39-gnumake` derivation that builds GNU Make 4.4.1 from a fixed source list with the boot3-backed `tcc-darwin-cc`, mirroring the Linux minimal-bootstrap manual compile approach instead of relying on a preexisting `make`.
- 2026-05-13: Validated `phase39-gnumake` end-to-end with the boot3-backed TinyCC path; the derivation compiles GNU Make 4.4.1, links a Mach-O executable, and runs `make --version` during the build.
- 2026-05-13: Next prerequisite sequence after `phase39-gnumake`: add a boot3-TinyCC `phase40-gnupatch`, then use `phase39-gnumake` plus `phase40-gnupatch` to attempt coreutils/bash-era tools before returning to a clean GCC 4.6 build. This keeps GCC source patching off the critical path until the host tools are real.
- 2026-05-13: Added and validated `phase40-gnupatch` from GNU Patch 2.5.9 using the boot3-backed TinyCC wrapper. The phase compiles the source list manually, links a Mach-O `patch`, and verifies `patch --version`; phase39 GNU Make still rebuilds cleanly against the expanded bootstrap libc.
- 2026-05-13: `phase40-gnupatch` can build and print its version, but applying even a one-hunk patch currently segfaults after diff detection. Kept `phase41-coreutils` moving by using the builder patch for source prep while retaining phase40 as the next tool-quality blocker to fix.
- 2026-05-13: Strengthened the prerequisite validation target: a normal `phase39-gnumake` invocation currently bus-errors before it can read even an empty makefile, so `phase41-coreutils` temporarily uses the builder `make` to keep exposing TinyCC/libc compile blockers while GNU Make runtime debugging remains open.
- 2026-05-13: Removed the ineffective GNU Make `MAKEFILES` source substitution from `phase39-gnumake`; it did not fix the normal makefile bus error and should not be kept as bootstrap patch debt.
- 2026-05-13: Added the missing `FNM_EXTMATCH` compatibility define to the bootstrap `fnmatch.h` so the coreutils stress build can proceed past `lib/fnmatch.o` and expose the next real TinyCC/libc blocker.
- 2026-05-13: Fixed the real GNU Make bus-error blocker at the Mach-O relocation layer: absolute data relocations that point at text labels now resolve to the executable text VM address instead of leaving TinyCC function-pointer initializers pointing into `__DATA`.
- 2026-05-13: Added a phase34 function-pointer relocation smoke test, covering both a global function pointer and a function pointer inside a global table; this directly protects the GNU Make function table failure mode.
- 2026-05-13: Fixed the `phase40-gnupatch` apply crash by returning a non-null static `struct tm` from the bootstrap `localtime`/`gmtime`, then added a real one-hunk patch-apply smoke test and wired coreutils source patching to bootstrapped GNU Patch.
- 2026-05-13: Added Darwin `rename(2)` to the bootstrap syscall table and set `stat(2)`/`access(2)` errno behavior enough for GNU Make and GNU Patch runtime smoke tests; coreutils now uses bootstrapped GNU Make explicitly via `-f`.
- 2026-05-13: Fixed recipe execution in bootstrapped GNU Make by adding Darwin `fork(2)`, `execve(2)`, and `wait4(2)` syscall wrappers, including the Darwin x86_64 fork child-return convention and a minimal `execvp` `/bin`/`/usr/bin` search path.
- 2026-05-13: `phase39-gnumake` now validates a real recipe execution instead of a dry run, and `phase40-gnupatch` still validates a real patch application after the fork/exec changes.
- 2026-05-13: Advanced `phase41-coreutils` to use only bootstrapped GNU Make and GNU Patch for its build driver/source patching; it now reaches `lib/gettime.o` after compiling 32 objects, with the next blocker being bootstrap header coverage for `struct timespec` and later libc/tool gaps.
- 2026-05-13: Coreutils stress build now reaches `lib/idcache.o` after 35 objects with bootstrapped make/patch and real recipe execution; added the next minimal passwd/group header and libc stubs needed for that path.
- 2026-05-13: Coreutils stress build advanced to `lib/makepath.o` after 41 objects; added the next required `passwd` fields, group header/stubs, `HAVE_SYS_TYPES_H`, and minimal ownership/permission definitions.
- 2026-05-14: Fixed the next real coreutils bootstrap blockers without adding source patches: `tcc-darwin-cc` now uses the Nix bash shebang directly so empty bootstrap environments cannot fall back to `/bin/bash`, syscall wrappers now translate `fork`/`execve`/`wait4` failures into `errno`, and the phase41 stress build advances to `lib/ftw.o` after 91 objects. Added only the libc/header surface proven by that path (`EXIT_*`, `ENAMETOOLONG`, `ENOSYS`, `settimeofday`, `strtod`, `getchar`, `fchdir`, `dirfd`). Phase39 GNU Make and phase40 GNU Patch still validate end-to-end with those changes.
- 2026-05-14: Advanced phase41 past `lib/ftw.o`; the next blocker is coreutils' replacement `lib/dirfd.o` conflicting with the bootstrap `dirfd` declaration. Removed the header declaration but kept the libc symbol for later linking, and revalidated phase39 GNU Make plus phase40 GNU Patch.
- 2026-05-14: Killed the stale phase41 build monitor and fixed the proven link blocker exposed at `src/basename`: added only the missing bootstrap `getuid` declaration/definition, while keeping the cctools `ar` override because bootstrapped TinyCC has no working `tcc -ar` mode yet.
- 2026-05-14: The `getuid`/cctools-`ar` checkpoint revalidated `phase39-gnumake` and `phase40-gnupatch` and moved phase41 from the prior `getuid` link failure to the next concrete link gap, `getgid`; added only that next gid stub/prototype for the next run.
- 2026-05-14: The `getgid` checkpoint moved phase41 to the next concrete link gap, `getegid`, still at the first linked coreutils binary (`src/basename`) after successfully compiling 97 objects and archiving `lib/libfettish.a`; added only that egid stub/prototype for the next run.
- 2026-05-14: The `getegid` checkpoint moved phase41 past the uid/gid link series and exposed a different real issue: TinyCC treated `FILESYSTEM_PREFIX_LEN`/`ISSLASH` in coreutils `ftw.c` as unresolved functions because that source only defines them under `_LIBC`; added command-line function-like macro definitions instead of patching coreutils source.
- 2026-05-14: The path macro checkpoint moved phase41 to the next concrete link gap, TinyCC's unsigned long-double-to-integer helper `__fixunsxfdi`; added that helper to the bootstrap libc rather than changing coreutils.
- 2026-05-14: The `__fixunsxfdi` checkpoint moved phase41 to the next concrete TinyCC runtime helper gap, `__floatundixf`; added only that unsigned-integer-to-long-double helper in bootstrap libc.
- 2026-05-14: The `__floatundixf` checkpoint moved phase41 to the next concrete POSIX runtime gap, `nanosleep`; added the minimal prototype and no-op bootstrap libc implementation needed by coreutils' `xnanosleep` path.
- 2026-05-14: The `nanosleep` checkpoint got phase41 past the first linked binary (`src/basename`) and into compiling `src/cat.o`; added a minimal `sys/ioctl.h` declaration header without defining optional ioctl feature macros such as `FIONREAD`, keeping the ioctl-dependent code paths disabled for now.
- 2026-05-14: The `sys/ioctl.h` checkpoint advanced phase41 through `src/cat`, `src/chmod`, and `src/cksum`; the next concrete header gap is `csplit` needing `SIGHUP`/`SIGQUIT`, so added only those signal constants.
- 2026-05-14: The `SIGHUP`/`SIGQUIT` checkpoint advanced phase41 through compiling and linking `src/csplit`; the next concrete runtime gap is `clearerr` while linking `src/cut`, so added only the standard no-op stream error reset stub and prototype.
- 2026-05-14: The `clearerr` checkpoint advanced phase41 through `src/cut`, `src/dirname`, `src/echo`, and `src/expand`; the next concrete runtime gap is `strcspn` while linking `src/expr`, so added only the libc string helper and prototype.
- 2026-05-14: The `strcspn` checkpoint advanced phase41 through `src/expr`, `src/fold`, `src/head`, `src/hostname`, `src/id`, `src/join`, and `src/kill`; the next concrete syscall gap is `link` while linking `src/link`, so added the Darwin raw syscall label and header prototype.
- 2026-05-14: The `link` checkpoint advanced phase41 through linking `src/link`; the next concrete syscall gap is `symlink` while linking `src/ln`, so added the Darwin raw syscall label and header prototype.
- 2026-05-14: The `symlink` checkpoint advanced phase41 through linking `src/ln` and `src/logname`; the next concrete mkfifo gap is `S_IFIFO` plus `mknod`, so added the FIFO mode bit, prototype, and Darwin raw syscall label.
- 2026-05-14: Batched the next obvious coreutils syscall/header surface instead of one-symbol rebuilds: added file type mode macros plus bootstrap libc/prototype coverage for ownership, sync, truncate, utimes, and getwd paths that appear in remaining coreutils sources.
- 2026-05-14: The impure checkpoint advanced through `src/mkfifo`, `src/mkdir`, `src/mknod`, and `src/nl`; the next runtime gap is stdio alias coverage in `src/od`, so batched `fgetc`, unlocked stdio aliases, and `fseeko`.
- 2026-05-14: The stdio alias checkpoint advanced to compiling `src/paste`, which instantiates sentinel `FILE` objects; made bootstrap `FILE` concrete in the generated header to match the libc representation.
- 2026-05-14: The concrete `FILE` checkpoint advanced through `src/paste` and `src/pathchk` to `src/pr`; added minimal `strftime` coverage for the remaining formatted time path.
- 2026-05-14: The `strftime` checkpoint advanced through `src/pr`, `src/printf`, `src/ptx`, and `src/pwd`; `src/readlink` exposes sys2 fallback declarations needing user/group typedefs, so added `uid_t`/`gid_t` to bootstrap `unistd.h`.
- 2026-05-14: The user/group typedef checkpoint advanced through `src/readlink` and `src/rmdir`; the next string helper gap is `strspn` while linking `src/seq`, so added it with the existing string prototypes.
- 2026-05-14: The impure `bootstrap-coreutils.mk` build completed after the batched Darwin syscall/header additions, including the larger `ls`, `install`, `cp`, `mv`, `rm`, and checksum targets.
- 2026-05-14: The real Nix `phase41-coreutils` build completed compilation and failed during install because the bootstrap hash table still used float tuning; added `patches/coreutils-hash-no-float.patch` to make coreutils hash growth/shrink integer-only.
- 2026-05-14: Replaced the fake regular-file-only `stat`/`fstat` shim with Darwin `stat64`/`fstat64`/`lstat64` syscalls and a matching bootstrap `struct stat`, because `install` and `test -d` need real directory mode bits.
- 2026-05-14: Real `stat64` made GNU make observe directories and exercise directory traversal, so the previous `opendir`/`readdir` stubs became invalid; added Darwin `getdirentries64` plus real `mkdir`/`rmdir`/`chdir` wrappers as the next anticipated directory syscall batch.
- 2026-05-14: Kept the public bootstrap `struct stat` ABI small and stable, and changed `stat64`/`fstat64`/`lstat64` wrappers to translate from Darwin's kernel layout into that ABI instead of exposing the kernel struct directly.
- 2026-05-14: Left bootstrap `stat` timestamps zero while preserving real type/mode/size metadata, because GNU make's early timestamp path still crashes before the runtime is ready for full Darwin timespec semantics.
- 2026-05-14: `phase39-gnumake` now builds and passes its smoke test again with real file type bits and zeroed bootstrap timestamps.
- 2026-05-14: The real Nix `phase41-coreutils` build now completes: `/nix/store/1jwf2hfl6adkghcwzay62vr2anhskpxc-darwin-minimal-bootstrap-phase41-coreutils-5.0-amd64`.
- 2026-05-14: Fresh `phase35-gcc46-all-gcc` after the coreutils checkpoint reached host libiberty and failed compiling recursive `regex.c`; fixed the `tcc-darwin-cc` wrapper to pass the original source directory as an include path whenever it copies a path-qualified C input to `.tcc-darwin-input-N.c`.
- 2026-05-14: The wrapper include-dir fix moved `phase35-gcc46-all-gcc` through recursive `regex.c` and further into host libiberty; the next blocker is bootstrap header coverage for libiberty `getopt.c`, so batched the standard declarations for `NULL`, `atoi`, `freopen`, and `strncat` plus the missing libc `strncat` implementation.
- 2026-05-14: `phase35-gcc46-all-gcc` now reaches libiberty `pex-unix.c`; the blocker was our bootstrap `sys/resource.h` exposing `struct rlimit` but not `struct rusage`, so added the Darwin-compatible `struct rusage` surface used by `wait4` without patching GCC sources.
- 2026-05-14: The `struct rusage` addition was not enough because GCC libiberty's `pex-unix.c` includes `sys/wait.h` for `wait4` without including `sys/resource.h` when `getrusage` is absent; fixed the bootstrap ABI surface by making `sys/wait.h` include `sys/resource.h` and prototype `wait4` with `struct rusage *`.
- 2026-05-14: `phase35-gcc46-all-gcc` now clears host libiberty and reaches bundled GMP generator programs; `gen-bases` exposed missing bootstrap `log(3)` and insufficient `%f` formatting in the tiny libc, so added a minimal `log` implementation and varargs `%f` output support for generated GMP tables.
- 2026-05-14: The first `%f` libc attempt regressed the earlier TCC stage because that compiler cannot compile real varargs definitions reliably; kept the harmless math/header improvements, reverted `printf`/`fprintf` to fixed bootstrap-call ABI, and patched only GMP's `gen-bases.c` generator inside `phase35` to avoid runtime `log` and `%f` varargs during the bootstrap.
- 2026-05-14: The early TCC crash was from helper symbols for double/integer conversion being inferred before their definitions after adding floating-point formatting helpers; added explicit helper prototypes at the top of the tiny libc so phase23 can compile it again.
- 2026-05-14: GMP then reached `gen-psqr`, whose bootstrap-built executable crashes before emitting `mpn/perfsqr.h`; for the x86_64/64-bit-limb bootstrap path, replaced only that generator with a deterministic emitter for the known 64/0 table generated from the same GMP source, keeping the rest of GMP built by the bootstrap compiler.
- 2026-05-14: GMP compile reached `mpz/cmp_d.c`, which requires the standard floating-point limit constants from `<float.h>`; expanded the bootstrap `float.h` with the usual binary32/binary64/x87 limit macros instead of patching GMP source.
- 2026-05-14: GMP reached `mpz/inp_str.c`; its internal `mpz_inp_str_nowhite` prototype was hidden because GMP did not recognize the bootstrap `<stdio.h>` as a stdio header. Added the conventional `_STDIO_H` guard macro to the generated header rather than patching GMP.
- 2026-05-14: GMP completed its library subdirectories and then failed trying to regenerate `doc/gmp.info` through GCC's missing `makeinfo` shim; seeded a placeholder GMP info file and forced `MAKEINFO=true` for the phase35 configure/make path so docs stay off the bootstrap critical path.
- 2026-05-14: MPFR advanced into library compilation and stopped at `get_d.c` because the bootstrap `<float.h>` still lacked `DBL_EPSILON`; added the standard epsilon macros for float/double/long double in the generated header.
- 2026-05-14: The rebuilt `phase35-gcc46-all-gcc` advanced through GMP and deeper into MPFR, then stopped at `out_str.c` because the bootstrap `<locale.h>` exposed `setlocale` but not `struct lconv`/`localeconv`; added the minimal C-locale ABI and also surfaced the already-observed `index`/`rindex` and `isascii` declarations to avoid implicit-int pointer truncation in the next long rebuild.
- 2026-05-15: The next `phase35-gcc46-all-gcc` run reached GCC `fixincludes` and stopped because `<unistd.h>` only exposed `F_OK`; added the standard `X_OK`/`W_OK`/`R_OK` access-mode constants there and guarded the bootstrap `<locale.h>` `CHAR_MAX` definition to remove the MPFR warning flood.
- 2026-05-15: An impure resume from the kept GCC build showed the next `fixincludes` blocker: GCC compiles path-qualified sources without an explicit `-o`, but the Darwin TCC wrapper copied them to `.tcc-darwin-input-N.c` and left TCC's default object under that temporary name; added wrapper logic to emit the normal basename object (`fixincl.o`, etc.) for single-input compile-only commands.
- 2026-05-15: The long `phase35-gcc46-all-gcc` rebuild advanced through GMP, MPFR, MPC, host libiberty, and `fixincludes`, then failed configuring `libcpp` because GCC's custom dependency-style probe requires `-MD`/`-MF`/`-MT` dependency output while the bootstrap TinyCC rejects those flags. Added dependency-flag handling to the Darwin TinyCC wrapper so configure can select a usable depmode without patching GCC/libcpp source.
- 2026-05-15: Restarted the depfile-validation rebuild with GCC phase make jobs driven by `NIX_BUILD_CORES` instead of hard-coded `-j1`, because every wrapper/libc iteration currently replays the full bundled GMP/MPFR/MPC path and serial rebuilds make the Darwin bootstrap loop unnecessarily expensive.
- 2026-05-15: The first parallel phase35 run exposed that `tcc-darwin-cc` used fixed `.tcc-darwin-input-N.c` names when copying path-qualified sources, so concurrent compiles in one directory deleted each other's temporary inputs. Moved those source copies under per-invocation `mktemp` directories while preserving original include-dir and default-object behavior.
- 2026-05-15: The depfile fix let phase35 pass the previous `libcpp/configure` blocker and begin compiling libcpp; the next real bootstrap ABI gap is `E2BIG` in `charset.c`, with `bsearch` also used there. Added Darwin `E2BIG` plus a tiny libc `bsearch` implementation/prototype instead of patching libcpp.
- 2026-05-15: Restarted the phase35 rebuild again after confirming MPC configure was spending minutes per `-lmpfr -lgmp` conftest relink. Added a `tcc-darwin-cc` archive-to-M1 cache keyed by archive checksum and shared through `TCC_DARWIN_CACHE_DIR` for phase35/phase36, so repeated configure links reuse converted GMP/MPFR/MPC archive members instead of expanding and translating every object on each invocation.
- 2026-05-15: The archive-cache phase35 rebuild reached GCC's build generators; `build/genmodes -m` succeeded but full `build/genmodes` corrupted tagged `printf("%*s")` output and then segfaulted. Fixed the tiny libc formatter to consume dynamic widths/standard printf fields and added the adjacent `rewind`, `asctime`, and `putenv` header surface exposed by the same GCC generator compile.
- 2026-05-15: The phase35 rebuild cleared the `genmodes` printf corruption and progressed through GMP, MPFR, MPC, and the early GCC generators, then failed linking `build/gengtype` with `Target label lexer_line is not valid`. The real blocker was GCC trying to regenerate `gengtype-lex.c` via the missing `flex` shim, leaving the build-local lexer source as only `#include "bconfig.h"`; seeded the pregenerated GCC lexer into `build/gcc/gengtype-lex.c` after configure and impure-validated by recompiling/relinking `gengtype` from the kept tree.
- 2026-05-15: The seeded `gengtype-lex.c` fix let phase35 pass the previous `Target label lexer_line is not valid` link/runtime failure and reach `build/gengtype -r gtype.state`; the new blocker was the tiny libc `fscanf` supporting `%s`/`%[]` only, so `fscanf("%d")` failed on the first integer in `gtype.state`. Added integer conversions to bootstrap `fscanf`, rebuilt `phase34-tinycc-darwin-cc`, smoke-tested `fscanf("%d")`, and impure-relinked `gengtype` far enough to prove the state parser now passes the integer read and reaches output-file creation.
- 2026-05-16: The next phase35 rebuild passed `build/gengtype -r gtype.state` and advanced to GCC generator links/runs. Fixed the concrete generator blockers in the bootstrapped TinyCC path: `tcc-darwin-cc` now treats `-lm` as satisfied by the bootstrap libc, the libc exports `atol`/`atoll` and `exp`, `qsort` is real instead of a no-op, malloc now packs small allocations in mmap arenas instead of burning one page per allocation, and `printf`/`fprintf`/`sprintf`/`snprintf` use the existing bootstrap varargs path so GCC generator `fprintf` calls with more than four substitutions do not corrupt the call frame. Rebuilt `phase34-tinycc-darwin-cc`, smoke-tested `-lm`/`atol`/`qsort`/large formatting, and impure-validated `genconditions` plus `genautomata` from the kept phase35 tree.
- 2026-05-16: The next concrete phase35 blocker was `build/genmodes`, which crashed inside `calc_wider_mode` while sorting `MODE_INT` records. The first bounded quicksort still failed with GCC's comparator shape because `cmp_modes` does not return zero for identical records, so replaced the bootstrap libc `qsort` with a simple non-recursive shell sort that does not recurse or rely on pivot equality. Rebuilt `phase34-tinycc-darwin-cc` as `/nix/store/w4647yk4s57amddwmqilwxczghn7l1id-darwin-minimal-bootstrap-phase34-tinycc-darwin-cc-amd64`, smoke-tested integer/pointer-array `qsort`, `-lm`, `atol`, and >4-argument formatting, then relinked the kept phase35 `genmodes` and verified both `genmodes -m` and full `genmodes` complete.
- 2026-05-16: The phase35 rebuild progressed past `genmodes`, `gengtype`, GMP, MPFR, MPC, and generator linking, then failed compiling generated `build/gencondmd.c` at `puts ("])")`. The immediate cause was GCC 4.6's generated `#if GCC_VERSION >= 3001`/`__builtin_constant_p` condition table: with TinyCC as the build compiler, the huge skipped table confused preprocessing and left the writer body malformed. Added `patches/gcc46-genconditions-tcc-safe.patch` and applied it to the GCC 4.6 bootstrap source so `genconditions` emits conservative `-1` condition values without compiling target predicates or using the GCC-only preprocessor gates. Validated the patched source by rebuilding `gcc46-darwin-bootstrap-src`, impure-rebuilding `genconditions`, regenerating a 3552-line `gencondmd.c`, and compiling it with the phase34 TinyCC wrapper.
- 2026-05-16: Fixed bootstrap libc `printf("%s")` truncating generated GCC machine-description code at 255 bytes; rebuilt phase34 TCC (`/nix/store/yd3ihbxvg65xpwn8j1k859hs4cgnx62j-darwin-minimal-bootstrap-phase34-tinycc-darwin-cc-amd64`) and confirmed regenerated `insn-emit.c` keeps full braced action code.
- 2026-05-16: Fixed bootstrap libc alternate-form hex formatting (`%#x`/`%#X`) after `genattrtab` emitted invalid masks like `1f`; rebuilt phase34 TCC (`/nix/store/njjmn3pcfmiiymc8qc0spq7xixv4mpz3-darwin-minimal-bootstrap-phase34-tinycc-darwin-cc-amd64`) and verified `%#x` plus long `%s` output.
- 2026-05-16: Completed the GCC 4.6 all-gcc bootstrap path through the boot3 TinyCC Darwin wrapper: replaced the slow/corrupt large-link path with streaming M1-to-hex2 plus fixed Mach-O segment layout, switched archive linking to iterative symbol-driven member resolution, added remaining Darwin syscall/libc/memory primitives used by GCC, and patched only the GCC host-side Darwin/PCH and real-number macro generation issues exposed under the bootstrap compiler. Impure `make all-gcc` and the Nix phase35 build both produce working Mach-O `xgcc`/`cc1`; `xgcc -B... -S` emits Darwin assembly under `MACOSX_DEPLOYMENT_TARGET=10.6`.
- 2026-05-16: Fixed the Nix-only `fixincludes` regression by patching GCC 4.6's hard-coded `NATIVE_SYSTEM_HEADER_DIR` in `gcc/Makefile.in` to the phase34 bootstrap header tree before GCC subconfigure, matching the impure all-gcc validation that previously required a manual Makefile edit.
- 2026-05-16: Fixed GCC builtin version macros at the root cause by implementing the bootstrap libc `sscanf` subset used by `parse_basever`; phase34 now parses `4.6.4` as `3 4 6 4`, and phase35 `xgcc -E -dM` reports the correct `__GNUC__`, `__GNUC_MINOR__`, and `__GNUC_PATCHLEVEL__` values.
- 2026-05-16: Proved phase36 impurely before codifying it: replayed the phase35 GCC 4.6 work tree under `/tmp/gcc46-phase36-impure`, replaced GCC's assembler driver with the bootstrapped TinyCC Darwin compiler path, normalized Darwin assembly directives to TinyCC/GAS-style ELF assembly, and built target `libgcc.a` plus `libgcov.a` as ELF relocatable archives.
- 2026-05-16: Kept phase36 bootstrap purity bounded: rejected host clang/cctools `as` as the final assembler path for this phase, while still using cctools `ar`/`ranlib` only as archive tooling until the bootstrap chain has a real replacement archiver. Deferred Darwin crt extras, EH/unwind objects, and SSE/FP helper objects rather than expanding GCC patch debt.
- 2026-05-16: Codified the impure phase36 fixes in Nix and validated `.#packages.x86_64-darwin.phase36-gcc46-libgcc`. The derivation now materializes the needed generated GCC headers, uses the bootstrapped TinyCC assembler wrapper, configures target libgcc directly, installs `libgcc.a`/`libgcov.a`, and verifies `_muldi3.o` plus `_gcov.o` have ELF magic `7f454c46`.
- 2026-05-17: Advanced phase36 from reduced integer/gcov libgcc to include GCC 4.6 target EH/unwind runtime objects. Impure replay exposed bootstrapped TinyCC assembler gaps for Darwin/GCC output (`cltq`, `movabsq`, and `.no_dead_strip`); normalized those in the phase36 assembler wrapper, restored `LIB2ADDEH*`, and validated the Nix build with `unwind-dw2.o`, `unwind-dw2-fde-darwin.o`, `unwind-sjlj.o`, `unwind-c.o`, and `emutls.o` present in `libgcc.a` as ELF relocatables.
- 2026-05-17: Modularized phase36 out of the monolithic `packages.nix` into `scripts/gcc46/phase36-libgcc.sh`, `scripts/gcc46/phase36-libgcc.py`, and `scripts/gcc46/phase36-bootstrap-as.awk`. Restored GCC 4.6 soft-fp TF helper coverage alongside EH/unwind, added the TinyCC assembler normalizations proven by impure replay (`movdqa`, `movss`, `xorps`, `divss`, and Darwin section/directive filtering), and validated the extracted Nix phase with `nix build .#packages.x86_64-darwin.phase36-gcc46-libgcc --print-build-logs`.
- 2026-05-17: Added `phase37-gcc46-bootstrap` as the first packaged usable GCC 4.6 C driver. The phase installs the bootstrapped `xgcc`/`cc1`, the phase36 `libgcc.a`/`libgcov.a`, a TinyCC-backed assembler bridge, and a `gcc` wrapper that compiles C to Darwin assembly, normalizes it to the bootstrap SysV/ELF object convention, links through the bootstrapped TinyCC Darwin linker, and validates `-S`, `-c`, C compile-link-run, plus a `__int128` libgcc-helper link/run smoke test. This required changing the phase36 assembly normalization to strip one Darwin symbol underscore and place GCC constants in `.data` so the existing ELF-to-M1 linker path can resolve them.
- 2026-05-17: Exposed the packaged GCC checkpoint as `.#packages.x86_64-darwin.gcc46-bootstrap` and as the x86_64 Darwin package default. `nix eval .#packages.x86_64-darwin.default.name --raw` and `nix build .#packages.x86_64-darwin.gcc46-bootstrap --no-link --print-out-paths` both resolve to the phase37 GCC 4.6 bootstrap output.
- 2026-05-17: Added phase36 installation of individual `libgcc.a` member objects under `libgcc-objects/` so the bootstrap can later replace archive extraction with symbol-selected object linking. A direct whole-object link attempt exposed an existing ELF-to-M1 synthetic-label gap in unused libgcc objects (`ELF_size_*`), so phase37 keeps the previously validated archive link path for now while still packaging the member objects; `nix build .#packages.x86_64-darwin.phase37-gcc46-bootstrap --print-build-logs` remains green.
- 2026-05-17: Replaced phase37's `libgcc.a` handoff with symbol-selected direct member-object linking. The packaged GCC wrapper now records `libgcc-symbols/*.tsv`, computes unresolved symbols from its own ELF objects with `tools/elf64-to-m1.py --symbols`, iteratively selects only needed `libgcc-objects/*.o`, and passes those objects to the bootstrapped TinyCC Darwin linker; this avoids libgcc archive extraction in the GCC driver path. Revalidated `nix build .#packages.x86_64-darwin.phase37-gcc46-bootstrap --print-build-logs` and an external `__int128` compile/link/run smoke exiting 42.
- 2026-05-17: Hardened phase37 validation so the derivation itself rejects a `gcc` wrapper that still mentions `libgcc.a`, then additionally compiles two separate C files to ELF objects and links them into a Mach-O executable that exits 42. Revalidated `nix build .#packages.x86_64-darwin.phase37-gcc46-bootstrap --print-build-logs`; the output keeps empty link/runtime stderr logs for `smoke`, `int128`, and `multi`.
- 2026-05-17: Added the packaged GCC 4.6 bootstrap output to the flake checks as `.#checks.x86_64-darwin.gcc46-bootstrap-smoke`, so the check set now has a direct gate for the phase37 compiler package and its embedded compile/assemble/link/run tests. Validated with `nix build .#checks.x86_64-darwin.gcc46-bootstrap-smoke --no-link --print-out-paths`.
- 2026-05-17: Started the next nixpkgs-style latest-GCC path by mapping local nixpkgs minimal-bootstrap exactly as GCC 4.6 C -> GCC 4.6 C++ -> GCC 10.4.0 -> GCC 15.2.0. A first impure GCC 4.6 C++ configure exposed a real phase37 wrapper bug: Autoconf probes like `-qversion` and no-input compiler checks fell through to the TinyCC linker and could spend minutes building empty Mach-O probes. Fixed phase37 to delegate `-V`/`-qversion`, initialize empty libgcc symbol sets, and fail no-input links immediately.
- 2026-05-17: The same C++ bootstrap probe showed `tcc-darwin-cc` itself also treated no-input linker probes as empty Mach-O links. Fixed the phase34 wrapper to reject no-input links immediately and to fail `-V`/`-qversion` without entering the linker. Validated `.#packages.x86_64-darwin.phase34-tinycc-darwin-cc` and confirmed the wrapper now returns `tcc-darwin-cc: no input files` without invoking M1/hex2.
- 2026-05-17: The GCC 4.6 C++ `all-gcc` replay then exposed two driver issues before it reached compiler sources: phase37 preprocessing inherited Darwin's modern default `MACOSX_DEPLOYMENT_TARGET=14.0`, which GCC 4.6 rejects, and every Autoconf no-helper link still scanned all packaged libgcc members even when there were no unresolved symbols. Fixed phase37 `-E` to force `MACOSX_DEPLOYMENT_TARGET=10.6` and short-circuit libgcc member selection when object symbols are already closed.
- 2026-05-17: Added the next two source checkpoints from the local nixpkgs minimal-bootstrap path: `phase42-gcc10-source` for GCC 10.4.0 plus GMP 6.2.1/MPFR 4.2.2/MPC 1.3.1/ISL 0.24, and `phase43-gcc-latest-source` for GCC 15.2.0 plus GMP 6.3.0/MPFR 4.2.2/MPC 1.3.1/ISL 0.24. Validated both source derivations with `nix build .#packages.x86_64-darwin.phase42-gcc10-source .#packages.x86_64-darwin.phase43-gcc-latest-source --no-link --print-out-paths --print-build-logs`.
- 2026-05-17: Cleaned the worktree of stale impure GCC/coreutils scratch artifacts instead of committing generated build trees. Started replacing the experimental C-built `hex0` seed with a committed, commented x86_64 Darwin Mach-O `hex0` byte source, following the live-bootstrap/stage0 pattern where the trusted seed is auditable bytes and the next tool is built by executing them.
- 2026-05-17: Split the bootstrap phase derivations out of `packages.nix` into per-phase files under `phases/`, keeping `packages.nix` as the source/dependency/context aggregator. Added line-by-line stage0-style commentary to `hex0/hex0-amd64-darwin.hex0`, documented the seed in `hex0/README.md`, and validated the hand-assembled seed self-host check plus `phase1-hex1`, `phase2-hex2`, and package/check evaluation after the split.
- 2026-05-17: Started the modern GCC chain after the phase split. Verified upstream GCC latest is now 16.1.0 (released 2026-04-30), updated `phase43-gcc-latest-source` to 16.1.0, and added explicit next build phases matching the nixpkgs minimal-bootstrap shape: GCC 4.6 C++ from GCC 4.6 C, GCC 10.4.0 from GCC 4.6 C++, and latest GCC from GCC 10.4.0.
- 2026-05-17: Rebuilt `gcc46-bootstrap` successfully from the new hand-written `hex0` seed after the phase split. The first `phase44-gcc46-cxx-bootstrap` run reached GCC 4.6 C++ configure and then failed before compiling C++ because phase39 GNU Make could not create its parallel job pipe; pinned the modern GCC build scripts to serial `make -j1` until the bootstrap ABI grows a real jobserver/pipe path.
- 2026-05-17: The serial `phase44-gcc46-cxx-bootstrap` replay then spent its time repeating GCC/libiberty/MPFR/MPC configure links through the slow hex2-backed Darwin linker. Stopped that run and seeded sub-configure caches from the already-proven `phase35-gcc46-all-gcc` work tree so phase44 can focus on the new C++/libstdc++ work instead of replaying known C configure probes.
- 2026-05-17: The first cache-seeded phase44 run exposed that Autoconf cache files also carry `ac_cv_env_*` entries pinning the old phase34 TinyCC environment and previous `/nix/var/nix/builds/...` paths. Kept the proven feature-test cache values but filtered those environment entries when importing phase35 caches into the GCC 4.6 C++ phase.
- 2026-05-17: The next phase44 run got past top-level configure but still spent minutes configuring GCC's bundled `intl` even though `--disable-nls` keeps gettext off the critical path. Replaced the disabled `intl` subdir with a no-op Makefile after top-level configure so C++ bootstrap time is spent on GCC/libstdc++ instead of unrelated NLS probes.
- 2026-05-17: The following phase44 run reached GCC subconfigure and exposed a bad assumption in our C++ phase: exporting `CXX=$CC` made GCC look for an existing C++ preprocessor even though the minimal-bootstrap path builds GCC 4.6 C++ from the C compiler alone. Removed the host `CXX`/`CXXCPP` exports from phase44 so GCC can use its normal "just compiled" target C++ path.
- 2026-05-17: Phase44 then reached the first real compile with `phase37-gcc46-bootstrap` and failed on `libiberty/regex.c` including `./ansidecl.h`; this is the same path-qualified-source include preservation issue previously fixed in the TinyCC wrapper. Fixed the phase37 GCC wrapper to add the original source directory as an include path for `dir/file.c` inputs.
- 2026-05-17: The first phase37 include-dir fix still passed path-qualified sources directly to GCC, which made quoted includes search the source directory before the build directory and broke `libiberty/config.h`. Adjusted the wrapper to compile a temporary basename copy and append the original source directory after existing include flags, preserving build-local `config.h` precedence.
- 2026-05-17: Diagnosed the current `phase44-gcc46-cxx-bootstrap` blocker as a bootstrap GCC 4.6 cpplib include-search defect: when `-I.` appears before source or sysroot include directories, a missing header in `.` becomes fatal instead of continuing to later include dirs. The next fix is to have the phase37 wrapper materialize a writable include overlay for `-I.` build directories, preserving build-local `config.h` while making later source/sysroot headers visible from the first include directory.
- 2026-05-17: The include-overlay fix moved `regex.c` past `<ansidecl.h>` in a direct wrapper repro and exposed the paired quoted-include side of the copied-source problem: local headers such as `xregex.h` must be visible next to the temporary basename copy. Added source-directory symlinks into the wrapper temp directory while keeping build-directory `config.h` precedence for angle includes.
- 2026-05-17: The direct `regex.c` repro then showed GCC 4.6's broken include search also treats the temporary source directory as the first fatal quoted-include location. Extended the phase37 overlay to link later include-dir entries into the temporary source directory as well as the writable build directory.
- 2026-05-17: The `regex.c` direct repro now reaches assembly normalization; GCC 4.6 emits Darwin `.static_data`, so extended the bootstrap assembler filter to lower it to the existing data-section convention rather than patching libiberty.
- 2026-05-17: The first full phase44 retry showed the include overlay can encounter existing generated symlinks such as `testsuite`; hardened the wrapper to treat existing symlinks as present so overlay setup is idempotent inside GCC build directories.
- 2026-05-17: The relative-path repro showed temp-directory overlay symlinks were broken when GCC passed relative include directories such as `../../src/include`. The wrapper now resolves temp overlay targets through the compile working directory while leaving build-directory symlinks relative.
- 2026-05-17: Phase44 advanced past `regex.o` into `cplus-dem.o`; the next assembler-normalization gap is Darwin `.const_data`, so lowered it to the same bootstrap data-section convention as `.static_data`.
- 2026-05-17: Phase44 then reached `concat.c` and failed because the phase37 wrapper exposed the TinyCC bootstrap `stdarg.h` to GCC 4.6; added the packaged GCC internal include directory ahead of the bootstrap sysroot in both the wrapper include overlay and `xgcc` invocation.
- 2026-05-17: Phase44 progressed through host libiberty and reached GCC subconfigure, where imported phase35 caches incorrectly pinned `CC`/`CPP` probes to the older TinyCC wrapper. Kept feature-test cache values but now filters compiler/preprocessor program cache entries so GCC subconfigure uses the phase37 GCC wrapper.
- 2026-05-17: The GCC subconfigure preprocessor sanity failure came from the same broken cpplib search semantics: putting GCC internal headers before the sysroot made `<stdio.h>` fail when absent from the first directory. Phase37 now builds a merged include overlay that prefers GCC internal headers such as `stdarg.h` and fills the rest from the bootstrap sysroot, then exposes that as one include directory.
- 2026-05-17: The merged include directory fixed GCC subconfigure but exposed GCC's wrapper `stdint.h`, which relies on `include_next`; because the bootstrap preprocessor cannot safely model multiple include roots yet, the merged overlay now skips GCC's `stdint.h` and uses the bootstrap sysroot `stdint.h` while still keeping GCC-only headers such as `stdarg.h`.
- 2026-05-17: Phase44 advanced deeper into host libiberty and stopped at `floatformat.o` because GCC emitted SSE2 `xorpd`; extended the assembler normalizer to lower it to the existing `pxor` encoding path like `xorps`.
- 2026-05-17: The next `floatformat.o` replay moved from `xorpd` to SSE2 scalar divide `divsd`; added the corresponding byte emission next to the existing `divss` normalizer.
- 2026-05-17: Rather than chase every SSE2 scalar double opcode emitted by GCC 4.6 for `floatformat.c`, the phase37 wrapper now compiles with `-mno-sse2`; the assembler filter maps the remaining SSE1 `movlps` spelling to the already-supported `movq` XMM move.
- 2026-05-17: The post-libiberty phase44 replay spent more than thirty minutes re-running bundled GMP configure probes through the slow hex2-backed linker. Stopped that run and expanded phase44 cache seeding to include the already-proven GMP, libcpp, libdecnumber, zlib, fixincludes, and build-libiberty configure caches, still filtering stale compiler program entries.
- 2026-05-17: Phase44 progressed past host libiberty into bundled GMP, then failed compiling GMP's generated `gen-fac_ui.c` because the GCC 4.6 cpplib bug treated a missing `stdio.h` in the source `-I../../src/gmp` directory as fatal before reaching the merged bootstrap header root. Extended the phase37 wrapper to materialize merged bootstrap headers into writable `-I` directories as well as the cwd/temp source overlay.
- 2026-05-17: Phase44 then passed GMP's `stdio.h` include boundary and built `gen-fac_ui`, but the generated helper segfaulted immediately in bootstrap `malloc`. Reproduced impurely with a tiny malloc smoke; the bootstrap Mach-O path does not reliably zero BSS/static-zero data, so the libc bump allocator's zero-initialized `malloc_cur`/`malloc_end` could start as garbage. Changed them to explicit `-1` sentinels in initialized data and made `malloc` recognize that first-call state.
- 2026-05-17: Rebuilt phase37 after the malloc sentinel fix and validated that a heap-allocation smoke now runs, but `gen-fac_ui` still crashed on `mpz_out_str`. Reduced that to `fputc('X', stdout)`: the bootstrap Mach-O path is also unsafe for loading the exported `stdout`/`stderr` data symbols. The phase34 sysroot now exposes `stdin`/`stdout`/`stderr` as the fd-valued `FILE *` constants that the libc already expects, avoiding those data-symbol loads in newly compiled build helpers.
- 2026-05-17: The rebuilt phase37 package passed its in-derivation checks, then an external `gcc file.c -o file` validation failed because the installed wrapper used `/usr/bin/env bash`; outside Nix on Darwin that can select bash 3.2, where `set -u` treats empty arrays as unbound. The phase37-generated `gcc` and assembler wrappers now embed the Nix bash path used by the builder so external bootstrap use gets the same shell semantics as the derivation.
- 2026-05-17: The next external phase37 validation reached the assembler and failed on `sall` even though the Nix build had normalized that opcode. Root cause was the assembler filter using GNU awk word-boundary regexes while the installed wrapper runs plain `awk` from the user's environment outside Nix. Rewrote the opcode normalizations to POSIX-awk-compatible start-of-line substitutions so the installed compiler behaves the same outside the derivation.
- 2026-05-17: Prepared the GCC10/latest bootstrap driver for the next phase while phase44 runs: modern GCC builds now explicitly use the staged cctools assembler/linker path and disable nonessential runtime libraries (`libatomic`, `libgomp`, `libitm`, `libquadmath`, `libssp`, and vtable verification), keeping the immediate target to the C/C++ compiler path instead of expanding the runtime surface prematurely.
- 2026-05-17: The next phase44 run reached GMP's `gen-fib` helper and failed linking with `Target label f is not valid`. Reproduced directly with the packaged phase37 compiler: GCC emitted Darwin `.comm _f`, but the assembler normalizer stripped underscores from code references while leaving `.comm` definitions as `_f`, making `f` look undefined. Fixed `.comm`/`.zerofill` symbol normalization instead of patching GMP.
- 2026-05-17: The `.comm` definition fix made `gen-fib` link, but reduced global-symbol smokes still crashed because the data symbol name `f` is also a valid hex token in the stage0/M1/hex2 syntax. Hardened `elf64-to-m1.py` to prefix all pure-hex symbol labels (for example `f` -> `ELF_f`) while preserving the original ELF symbol names for dependency resolution.
- 2026-05-17: The pure-hex label hardening made `f` addressable, but the reduced global-pointer smoke still crashed after `puts("a")`. Root cause was the assembler filter deleting `@GOTPCREL` and turning GCC's address load (`movq f@GOTPCREL(%rip), %rax`) into a value load; lowered those GOT address loads to `leaq f(%rip), %rax` instead.
- 2026-05-17: Switched GCC 4.6 C++/GCC 10/latest iteration to a persistent impure runner at `scripts/impure/build-gcc-phase.sh`; it reuses Nix-built phase inputs but keeps local `work/impure/phase44-gcc46-cxx`, `phase45-gcc10`, and `phase46-gcc-latest` trees so compile/link failures can be debugged without replaying each derivation from scratch.
- 2026-05-17: Added `BOOTSTRAP_MAKE` and `BOOTSTRAP_JOBS` overrides to the GCC 4.6 C++ and modern GCC scripts. Nix derivations still default to serial phase39 make, while the impure runner now uses host GNU make with bounded parallelism to avoid paying the phase37 compile cost one object at a time during debugging.
- 2026-05-17: The first parallel impure phase44 run exposed a phase37 wrapper race rather than a compiler failure: concurrent compilations could both pass `test -e` and then one `ln -s` would fail for shared header links such as `floatformat.h` and `assert.h`. Added a race-tolerant `ensure_symlink` helper to the generated phase37 GCC driver before retrying with a rebuilt phase37.
- 2026-05-17: The parallel impure phase44 retry advanced into `fixincludes`, `libcpp`, and GMP. The next blockers were concrete: internal PC-relative jump table relocations in `libiberty/regex.o` produced synthetic labels that can land inside instructions, `libcpp/lex.c` emitted unsupported MMX/SSE `pmovmskb`, and the bootstrap `stdlib.h` did not expose `NULL`. Fixed the ELF-to-M1 converter to materialize same-section PC32 relocations as numeric 32-bit displacements, disabled GCC 4.6 libcpp's vectorized lexer for this bootstrap source, and added `NULL` to bootstrap `stdlib.h`.
- 2026-05-17: Switched the immediate GCC 4.6 C++ retry back to an impure path after the patched phase35 Nix rebuild spent most of its time replaying configure conftests. Rebuilt a local phase37 wrapper in `work/impure/phase37-newconv/out` against the updated phase34 compiler and current `tools/elf64-to-m1.py`, and made phase44 defensively disable GCC 4.6 libcpp's x86 vector lexer when reusing an older phase35 source tree.
- 2026-05-17: The impure phase44 retry progressed past configure, fixincludes, GMP's `NULL` blocker, and the previous libcpp `pmovmskb` blocker, then failed on `libcpp/symtab.o` because GCC 4.6 still emitted SSE `movaps` with only `-mno-sse2`. Tightened the phase37 wrapper to compile bootstrap objects with `-mno-sse -mno-sse2 -mno-mmx` so the M1/TCC assembler path does not need SSE opcode support.
- 2026-05-17: Backed out the global `-mno-sse` experiment after build-libiberty hit `SSE register argument with SSE disabled` in `_doprnt.c`. Kept GCC's x86-64 ABI expectations intact and instead taught the bootstrap assembler filter to lower register-to-register `movaps`/`movdqa` copies to raw opcode bytes, which targets the actual TinyCC assembler gap without disabling SSE globally.
- 2026-05-17: The next impure phase44 run reached deeper into bundled GMP and failed in `mpz/get_str.c` because GMP itself uses `NULL` without pulling in the bootstrap `stdlib.h`. Added a small bootstrap `NULL` guard to bundled `gmp-impl.h` in both the impure phase44 source preparation and the phase35 source-prep derivation.
- 2026-05-17: The following impure phase44 run showed wrapper overlay pollution in bundled MPFR: the phase37 driver was symlinking all files from earlier include roots into writable build directories, so MPFR inherited GMP build-system files such as `Makefile.in` and `gmp-h.in`. Restricted build-directory overlays to header-like files and known header directories so generated source trees keep their own Makefiles.
- 2026-05-17: Phase44 was also still able to discover ambient host C++ tools during configure. The impure C++ phase now unsets inherited `CXX`/`CXXCPP`/`CXX_FOR_BUILD`, prepends failing `c++`/`g++`/`clang++` stubs, and supplies only a phase37-backed C++ preprocessor shim for configure sanity checks.
- 2026-05-17: The GMP generator sources then exposed the other side of include staging: adjacent source includes like `#include "dumbmp.c"` must be available next to the temporary basename copy, but not sprayed into writable build directories. Split phase37 staging into source-neighbor temp links for `.c`/C++/assembly files and header-only overlays for include/build directories.
- 2026-05-17: Phase44 then advanced through bundled GMP generator links and into GCC `fixincludes`; `fixincl.c` includes the generated `fixincl.x` table beside the source file, so the phase37 temp source-neighbor staging now also admits adjacent `*.x` generated include files without broadening build-directory overlays.
- 2026-05-17: After the `fixincl.x` fix, the impure parallel phase44 retry passed `fixincludes` compilation but exposed a dirty-tree/parallel GMP libtool race where `libmpn.la` observed `fib_table.lo` before the generated libtool object was usable. The C++ phase now builds bundled GMP serially before the parallel top-level make and clears the generated GMP table objects when resuming an impure tree.
- 2026-05-17: Fresh phase44 trees do not have `gmp/Makefile` immediately after top-level configure; GCC creates it through the `configure-gmp` make target. Phase44 now runs `configure-gmp` serially first, then builds bundled GMP serially, before allowing the rest of GCC/libstdc++ to run with the impure job count.
- 2026-05-17: A fully serial bundled GMP prebuild made impure phase44 iteration too slow. The protective split remains (`configure-gmp`, then build GMP, then top-level GCC), but the GMP build now honors `BOOTSTRAP_JOBS` after stale duplicate phase44 writers are killed.
- 2026-05-17: Moved the append-only bootstrap checklist/running log from `plan.md` to `todos.md`; continue recording impure phase44 iterations here and keep Nix validation deferred until the impure C++ path stabilizes.
- 2026-05-17: Phase44 impure C++ run reached `configure-mpc`; the immediate blocker was MPC probing `-lmpfr` before a stable bootstrap-visible MPFR archive, so phase44 now prebuilds MPFR after GMP and patches only MPC's MPFR configure link probe while leaving the actual library build in place.
- 2026-05-17: The impure phase44 rerun exposed another iteration-speed issue: the bootstrapped phase39 `make` can sit after `configure-gmp` completes, so impure debugging now uses `/usr/bin/make` via `BOOTSTRAP_MAKE` and leaves phase39/Nix validation for the stabilized pass.
- 2026-05-17: To keep phase44 iteration impure and fast, phase44 now seeds GMP/MPFR/MPC build directories from the proven phase35 checkpoint and overrides their top-level configure/build/install targets as no-ops; full prerequisite rebuilds are deferred until the C++ boundary is stable.
- 2026-05-17: Phase44 advanced past GMP/MPFR/MPC with seeded prerequisites and stopped in libcpp because GCC dependency files referenced the wrapper's temporary copied source path; the phase37 GCC wrapper now rewrites `-MF` dependency files back to stable source paths and strips temporary bootstrap directories.
- 2026-05-17: Killed overlapping phase44 impure runs after a rerun collided with the previous `make all-gcc`; continue with exactly one phase44 process against the current partially built workspace.
- 2026-05-17: Further reduced phase44 impure iteration by seeding phase35 host/build support directories (`libiberty`, `fixincludes`, `zlib`, `libcpp`, `libdecnumber`) and overriding their top-level targets; phase44 now focuses on the GCC/C++ frontend boundary rather than reconfiguring proven C support libraries.
- 2026-05-17: Phase44 reached GCC build-generator compilation and `genmodes.c` failed because the wrapper copied the source into a temp directory without GCC subdirectories like `config/i386`; the phase37 wrapper now symlinks GCC source-neighbor directories into the temp compile area.
- 2026-05-17: Phase44 got into GCC generator execution and `build/genenums` segfaulted; phase44 now seeds generated GCC MD/GC files and stamps from phase35 so the C++ pass can avoid re-running fragile TinyCC-built generators.
- 2026-05-17: Seeded generator outputs alone was not enough because GCC `Makefile` regenerated them when prerequisites were newer; phase44 now appends bootstrap stamp overrides to `src/gcc/Makefile.in` and also seeds `mddeps.mk`/`build/gencondmd.c`.
- 2026-05-17: Confirmed no stale phase44/GCC wrapper processes remain after killing the overlapping impure runs; continuing with one phase44 retry and leaving the log append-only in `todos.md`.
- 2026-05-17: Phase44 generator-object compilation failed resolving `config/vxworks-dummy.h` from temporary-copied GCC sources; fixed the phase37 wrapper to add its temp source-neighbor directory to the include path, so nested GCC config includes can resolve the symlinked `config/` tree.
- 2026-05-17: The phase37 `-I$tmpdir` fix was insufficient because GCC 4.6 reports nested quoted config includes relative to `config/i386`; changed the wrapper to materialize a temporary `config/` overlay with each config subdirectory containing a `config -> ..` symlink, avoiding source-tree mutation while satisfying those legacy include paths.
- 2026-05-17: Phase44 passed the GCC config include blocker and reached `gengtype`, which emitted parse errors on `line-map.h` and then kept running; tightened phase44 to seed `gtype.state` plus `gtype-*.h` from phase35 and override `s-gtype` so the C++ pass does not execute that fragile generator.
- 2026-05-17: Updated the top of `todos.md` with the active phase44 status/resolved items. The immediate blocker is not GCC source anymore: the impure phase37 wrapper still hardcodes a garbage-collected phase34 TinyCC store path, so rebuild the wrapper against the current phase34 before the next retry.
- 2026-05-17: The impure phase37 rebuild initially tried `builtins.currentSystem` (`aarch64-darwin`) and found no bootstrap package attrs; fixed `scripts/impure/build-gcc-phase.sh` to default bootstrap package lookup to `x86_64-darwin`, matching the amd64 Darwin chain being built.
- 2026-05-17: Avoided a slow accidental Nix rebuild after `phase34` lookup wanted to rebuild the whole seed chain from dirty sources; switched the impure phase37 rebuild to already-realized store paths for current phase34/35/36 instead.
- 2026-05-17: Rebuilt `work/impure/phase37-newconv/out` against current realized phase34/35/36 stores; the stale garbage-collected phase34 path is gone. A manual stdin-based smoke was invalid for this wrapper and was stopped; continue validation through the phase44 retry.
- 2026-05-17: Marked the live status at the top of this file as requested. The stale phase34 wrapper blocker is resolved, and the current phase44 retry includes a patch that seeds phase35 `build/gen*` binaries so GCC does not relink fragile generator programs such as `genmodes` through the bootstrap assembler path.
- 2026-05-17: Phase44 moved past generator relinking; the explicit `build/gen*` target overrides are active and generator stamps now reuse phase35 outputs. The next failure is `g++spec.o`: GCC's copied-source include staging still left an include-dir-provided `config` as a plain symlink, so `config/i386/i386.h` looked for `config/i386/config/vxworks-dummy.h`. Patched the phase37 wrapper to expand `config/` overlays after include-dir staging as well as after source-neighbor staging.
- 2026-05-17: Rebuilt the impure phase37 wrapper after the include-dir `config/` overlay fix and directly validated the previous `g++spec.o` failure command. The wrapper now compiles `../../src/gcc/cp/g++spec.c` to an object in the existing phase44 GCC build tree, so the next phase44 retry should resume past that include blocker.
- 2026-05-17: The next phase44 retry passed the previous `g++spec.o` blocker and failed in frontend sources where generated/root headers include nested source paths: `all-tree.def` needs `ada/gcc-interface/ada-tree.def`, and `c-family/c-common.h` needs root headers while being included from a copied `c-family/` directory. Extended the phase37 temp overlay to merge known GCC source directories recursively and hydrate every nested overlay directory with root/bootstrap headers.

## Bootstrap Log - 2026-05-17

- Confirmed the non-recursive source-tree/header overlay is sufficient for the previous `c-family/stub-objc.c` failure: the phase37 wrapper produced `/tmp/stub-objc-test.o` without the `c-family/*.def` or `splay-tree.h` quoted-include misses.

- 2026-05-17: Shortened the GCC46 impure loop. `PHASE44_RESUME=1` now reuses the existing top-level configure state, `PHASE44_MAKE_DIR=gcc PHASE44_TARGETS='...' PHASE44_SKIP_INSTALL=1` iterates individual GCC targets, and `PHASE37_SKIP_SELF_TESTS=1` rebuilds the wrapper without replaying link/run smoke tests. The phase37 wrapper no longer performs recursive overlays or CWD symlinking and avoids staging for plain `conftest.c` inputs.
- 2026-05-17: Added `GCC46_BOOTSTRAP_OBJECT_FORMAT=macho` to the phase37 wrapper. In that mode it assembles x86_64 Mach-O objects with `/usr/bin/as`, links temporary build tools through `/usr/bin/cc`, and injects a small Darwin stdio compatibility object for TinyCC-style `_stdin`/`_stdout`/`_stderr` globals. This is an impure iteration path; the pure path still defaults to ELF objects.
- 2026-05-17: Verified the resumed phase44 per-target loop with Mach-O objects: `c-lang.o` and `c-family/stub-objc.o` now build successfully as Mach-O x86_64 objects. The previous quoted include failures for `c-family/*.def`, `splay-tree.h`, and generated `gtype-desc.h` are fixed by source/build header overlays.
- 2026-05-17: The Mach-O prerequisite rebuild passed `libiberty` and `zlib`, then reached GMP `printf/repl-vsnprintf.c` and failed because the phase35 GMP configure cache recorded `ac_cv_func_strnlen=no` after a phase34 TinyCC probe failed on the symbol name `strnlen`. Phase44 now fixes the copied GMP config to use Darwin's `strnlen`, avoiding GMP's static fallback declaration conflict with SDK `string.h`.
- 2026-05-17: Added `PHASE44_REBUILD_MACHO_PREREQS_LIST` so impure retries can resume at the failed prerequisite instead of rebuilding already-confirmed Mach-O archives such as `libiberty` and `zlib`.
- 2026-05-17: GMP `make clean` can regenerate or restore the stale `HAVE_STRNLEN` result before rebuilding `printf/repl-vsnprintf.o`; phase44 now reapplies Darwin prerequisite config fixes after each clean and passes `-DHAVE_STRNLEN=1` on the GMP rebuild command line.
- 2026-05-17: The Mach-O GMP archive now rebuilds fully. The next prerequisite blocker is MPFR trying to run automake through a stale `/nix/var/nix/builds/.../missing` path; phase44 now refreshes bundled prerequisite generated-file timestamps so impure rebuilds do not enter maintainer regeneration rules.
- 2026-05-17: MPFR also tried to re-run `config.status --recheck` through the old Nix build directory after `make clean`. Phase44 now invokes prerequisite rebuild make with `-o Makefile -o config.status` so copied configured trees do not refresh through stale Nix build paths.
- 2026-05-17: MPFR `all` still descends into `tests` and stale maintainer rules. The Mach-O prerequisite rebuild now targets only `libmpfr.la`, which is the archive GCC needs for frontend linking.
- 2026-05-17: MPFR `libmpfr.la` now rebuilds as Mach-O. MPC hit the same stale top-level recursive refresh path, so phase44 now rebuilds `mpc/src/libmpc.la` directly instead of top-level `mpc all`.
- 2026-05-17: MPC `src/libmpc.la` now rebuilds, then `libcpp/charset.c` exposed modern Clang's implicit-function-declaration hard error for GCC 4.6's `fputs_unlocked` macro path. Phase37 strips fatal implicit-declaration `-Werror` variants, and phase44 passes `-Wno-implicit-function-declaration` for the Mach-O libcpp prerequisite archive.
- 2026-05-17: The Mach-O prerequisite rebuild now completes through `libcpp` and `libdecnumber`; resuming the direct phase44 GCC frontend build against rebuilt Mach-O prerequisite archives.
- 2026-05-17: The first resumed frontend build failed because the Mach-O prerequisite rebuild had deleted seeded `gcc/build/*.o`, causing generator support objects to fall back to ELF copies or missing files. Phase44 now preserves `gcc/build/*.o` during prerequisite rebuild cleanup; the live impure tree regenerated `read-md.o` and `genconstants.o` as Mach-O before resuming.
- 2026-05-17: The frontend then linked against an MPFR archive that still contained old ELF members from before the clean; the clean MPFR rebuild exposed missing Darwin `UINTMAX_MAX`/`INTMAX_*` macros through MPFR's `fits_uintmax.c`. Phase44 now defines those limits for the MPFR Mach-O prerequisite rebuild.
- 2026-05-17: `cc1` and `cc1plus` now build, but install failed because the direct GCC target list built the C++ frontend phony without the `g++` driver binary. Phase44 now includes `g++` in the default direct GCC target list.
- 2026-05-17: Phase44 now packages the GCC 4.6 C and C++ frontends from the Mach-O impure tree. The package step builds `libgcc.a`, installs `gcc`/`g++` wrappers that force a GCC-4.6-compatible `MACOSX_DEPLOYMENT_TARGET=10.8`, and copies the internal GCC headers so `cc1`/`cc1plus` are discoverable from the packaged prefix.
- 2026-05-17: Target runtime packaging needed a writable sanitized phase34 header sysroot: phase44 now copies `tcc-darwin-bootstrap` headers into `build/bootstrap-target-include`, removes duplicate `div_t`/`ldiv_t` declarations from `sys/types.h`, and adds the missing `size_t` typedef for `time.h`/`strftime` while preserving the original phase34 store path.
- 2026-05-17: Smoke-tested the packaged phase44 compiler by compiling C and C++ translation units with `out/bin/gcc` and `out/bin/g++`; both outputs are Mach-O x86_64 objects and report GCC 4.6.4. Linking remains intentionally separate from this checkpoint because the bootstrap C++ runtime surface is only just stabilized.
- 2026-05-18: Cleaned transient bootstrap scratch from `/tmp` again before GNU Hello validation; `/tmp` itself is now empty, but root filesystem pressure remains from repo/work and Nix store contents rather than live temp files.
- 2026-05-18: Promoted the GCC 16 handoff wrapper by using the real `16.1.0` runtime path, adding package/sysroot and SDK include roots, translating Darwin `-Wl,-syslibroot,...` arguments for direct `/usr/bin/ld`, defaulting direct links to SDK `libSystem`, and gating impure host shortcuts behind `GCC_MODERN_WRAPPER_HOST_SHORTCUTS`.
- 2026-05-18: Rebuilt a phase47 GCC 16 handoff from the phase46 GCC 16 package and validated it with a Mach-O C link/run smoke. The successful `all-gcc` pass still used the wrapper's impure host shortcuts for GCC source compiles; a strict `GCC_MODERN_WRAPPER_HOST_SHORTCUTS=0` replay remains the purity check before freezing.
- 2026-05-18: Extended the bootstrap Darwin header surface needed by real GNU package builds: minimal `wchar.h`/`wctype.h`/`xlocale.h`, `AvailabilityMacros.h`, Darwin `F_GETFL`, `strerror_r`, `strnlen`, `getprogname`, wide-character declarations, C locale limits, `SIZE_MAX`, `PTRDIFF_MAX`, `st_birthtimespec`, and a Darwin-compatible `FILE` layout for gnulib `fpending`.
- 2026-05-18: Started the strict phase47 GCC16 self-build with `GCC_MODERN_WRAPPER_HOST_SHORTCUTS=0`; the resumed build reached host/build libiberty and exposed stale configure positives for non-Darwin `sys/prctl.h`, `process.h`, and `vfork.h`. Patched the GCC latest source preparation and cache answers to keep those headers disabled on Darwin, and codified real Darwin `__stdinp`/`__stdoutp`/`__stderrp` stdio globals in phase34 so GNU packages do not inherit fd-valued `FILE *` constants.
- 2026-05-18: Checked `~/Git/nixpkgs`: this checkout's `gcc_latest` is GCC 15.2.0, not GCC16, and Darwin builds it under `gccStdenv` with a full Apple SDK sysroot plus external GMP/MPFR/MPC/zlib/cctools/libSystem dependencies. That confirms our missing-header/library churn is from the reduced compiler-only bootstrap recipe, not because GCC cannot self-build on Darwin.
- 2026-05-18: Corrected the strict-run harness so `GCC_MODERN_WRAPPER_HOST_SHORTCUTS` is exported into configure/make children; a manual resume without that env had accidentally re-enabled wrapper clang shortcuts and produced the misleading MPFR `_Float128` failure.
- 2026-05-18: Removed the modern GCC phase default `-Dwint_t=int`; the reduced sysroot now has a real `wint_t` typedef, so the command-line macro corrupts system declarations as `typedef int int` and blocks strict MPFR compilation.
- 2026-05-18: Built GNU Hello 2.12.2 with the phase47 GCC 16 wrapper. `-O2` currently links but exits 139, while a clean `-O0 -g0` rebuild produces a Mach-O x86_64 executable that runs `Hello, world!`, `--help`, and `--version` correctly against `/usr/lib/libSystem.B.dylib`; next task is isolating the `-O2` codegen/runtime mismatch before claiming optimized self-host package builds.
- 2026-05-18: Completed the strict phase47 GCC16 self-build using the phase46 GCC16 package with `GCC_MODERN_WRAPPER_HOST_SHORTCUTS=0`, `GCC_MODERN_HOST_BUILD_CC=0`, and `GCC_MODERN_TARGETS=all-gcc` expanded to `all-libcody all-gcc`. The build packages `work/impure/phase47-gcc16-strict/out` and its wrappers report `gcc-latest bootstrap compiler 16.1.0`.
- 2026-05-18: Fixed strict self-host blockers found during the phase47 replay: added Darwin `ftw.h`/`getopt.h` declarations, `truncate`, real Darwin `F_DUPFD`/`F_GETLK`/`F_SETLK`/`F_DUPFD_CLOEXEC` fcntl constants, Darwin-layout `FILE`, and stale ABI cleanup for rebuilt `libcpp`/`libcody` archives.
- 2026-05-18: Fixed the phase47 wrapper link path to add its packaged `libgcc`, `libstdc++`, and `libsupc++` automatically for direct `/usr/bin/ld` links; strict C and C++ smoke programs now compile, link, and run without enabling host shortcuts.
- 2026-05-18: Added `scripts/impure/build-gnu-hello.sh` as the repeatable GNU Hello proof. It builds GNU Hello 2.12.2 at `-O2 -g0` with strict phase47 GCC16, uses Darwin-compatible `ARFLAGS=rc`, verifies `Hello, world!`, `--version`, and `--help`, and produced SHA256 `00b6c4f068eeb111f0bc90ff69e3256c3e45fa97ec368ae8aa5ddfc7f4955f89`.
- 2026-05-26: Zero-python milestone. All 15 *.py files deleted; chain end-to-end uses M1 macro-asm + perl + M2-Planet C only. New M2-Planet C ports compiled via phase5-m2 → phase9-m1 → phase10-hex2 → Mach-O binary: `phase11b-m1-to-hex2` (replaces scripts/stage0/m1-to-hex2.pl, wired into phase34 tcc-darwin-cc), `phase11c-hex2-data-relocs` (replaces hex2-data-relocs.pl, wired into phase30/31/33 + tinyccSelfLinkCandidate), `phase11d-cc-arch-helper` (port + patch modes byte-identical to perl on cc_arch-darwin; not yet wired because phase4-cc-arch → phase11d → phase5-m2 → phase4 dep cycle blocks it). `elf64-to-m1.M1` gained blanket synth-label pre-pass for non-LOCAL .data and .bss symbols (addends {1,2,8,16,…,min(size,0x300),0x40C,0x4D0}); fixed an SIGSEGV on empty `.text` sections via a `[rbp-40] == 0` guard. gnu-hello-hash-comparison re-verified at the unchanged baseline `5019a64510837fae43fc7238b506ec11011542432c792b4ab7683db2e7ff2f73`. Remaining perl is intentional: phase1/2 hex0 builders (would need hand-written hex0 — task #29), phase4-cc-arch + phase5-amd64-m2 callers in phases 5–10 (cycle-blocked), and phase13/35/36/39 text-edit helpers.
- 2026-05-26: phase5-amd64-m2.pl cycle broken via `phase11e-macho-patcher-early`. Built tools/macho-patcher.M1 (M1 macro-asm) using ONLY phase3-m0 + phase2-hex2 — no phase5-m2 dependency. Key insight: M0 doesn't support M1's `!0xXX` raw-byte or `%0xNNN` 32-bit immediate-literal syntax, but a 5-line perl one-liner rewrites `!0xXX → BYTE_XX` and `%0xNNN → 4 BYTE_NN` LE bytes, then M0 + amd64_byte_defs.M1 (256 single-byte DEFINE macros) handles the rest. Resulting binary is BYTE-IDENTICAL to phase26g-macho-patcher (M1+phase10-hex2 path) — SHA256 `6112ffa79eab09144471675bc9b4c18a54161b676e5683e8b466c098160877b6`. Wired into phases 5/6/7/8/9/10 replacing `perl phase5-amd64-m2.pl patch X Y` with `${phase11e-macho-patcher-early}/bin/macho-patcher m2-segments X Y`. Removed perl from those phases' nativeBuildInputs. Deleted scripts/stage0/phase5-amd64-m2.pl, hex2-data-relocs.pl, m1-to-hex2.pl (zero callers each). Build time for phases 5-10 end-to-end is 23s; phase11e itself builds in ~5s. Initial Nix integration was hung 18h because of using -f/--base-address flags that phase2-hex2 doesn't support; fix is to concatenate template via phase2-catm first (MACHO-amd64.hex2 → final.hex2).
- 2026-05-26 (verification): Confirmed M0-built patcher is a drop-in for the chain.  After the cycle break, `phase9-m1` (M1 macro asm) is BYTE-IDENTICAL to its prior perl-built version (SHA256 `ea5c7ff0631143929f67df85aeb7fe247b619f66c7735527450bfcb3d4949d13`) and `phase10-hex2` (full hex2 linker) is identical (SHA256 `8c8b68fe91226abde7617a198c6d6dac75b0861edff84536311e7c801a281cf8`).  Strong evidence that downstream artifacts will continue producing the gnu-hello-hash-comparison baseline `5019a64510837fae43fc7238b506ec11011542432c792b4ab7683db2e7ff2f73`.
- 2026-05-26 (perl reduction): four perl-in-Nix invocations replaced with pure POSIX awk: phase11e's `!0xXX`/`%0xNNN` syntax rewriter → `scripts/stage0/m1-to-m0-syntax.awk`; phase2-catm's `port-catm-darwin.sh` → `scripts/stage0/port-catm-darwin.awk`; phase3-m0's `port-m0-darwin.sh` → `scripts/stage0/port-m0-darwin.awk`; phase4-cc-arch port-mode `perl phase4-amd64-cc-arch.pl port` → `scripts/stage0/port-cc-arch-darwin.awk`.  All four awk scripts produce byte-identical output to their perl predecessors; phase11e, phase2-catm, phase3-m0, phase4-cc-arch binaries all hash-match prior builds.  perl removed from nativeBuildInputs of phase11e/phase2-catm/phase3-m0; phase4-cc-arch still has perl for the patch step (RIP-rel32 binary fixup with 16 opcode patterns — needs M1 macho-patcher cc-arch mode or new M1 helper to fully eliminate).  Remaining perl in our scripts: scripts/stage0/phase1-amd64-hex1.pl (Mach-O hex1 builder, 270 lines), scripts/stage0/phase2-amd64-hex2.pl (Mach-O hex2 builder, 267 lines), scripts/stage0/phase4-amd64-cc-arch.pl patch mode (~90 lines).  All three need M1 macro-asm replacements assembled through the phase11e cycle-break chain.  Third-party perl (GCC autoconf, GMP build, mes-source git tarball massaging, gnumake autoconf) is unavoidable and out of scope.
- 2026-05-26 (end-to-end verification): gnu-hello-hash-comparison ran the full bootstrap with the phase5-amd64-m2.pl cycle-break in place.  Result: `phase46_gcc_latest=5019a64510837fae43fc7238b506ec11011542432c792b4ab7683db2e7ff2f73`, `phase47_gcc_latest_strict=` same hash, `phase46_phase47_equal=yes`.  Hash matches the pre-cycle-break baseline byte-for-byte — phase11e is fully validated as a drop-in replacement for `perl phase5-amd64-m2.pl patch X Y` through the entire chain (stage0 → mes → tcc → gcc46 → gcc10 → gcc-latest → strict re-bootstrap → gnu-hello).
- 2026-05-26 (zero-perl stage0): all three stage0 perl helpers eliminated. (1) phase4-amd64-cc-arch.pl deleted — the patch step now uses phase11e-macho-patcher-early m2-segments mode after injecting `:ELF_data` into cc_arch source via the port awk (output byte-identical, cc_arch-darwin SHA 4f1ca350...). (2) phase1-amd64-hex1.pl moved to scripts/stage0/legacy/ — replaced by hand-rolled hex0/sources/hex1_AMD64_darwin.hex0 (Mach-O header + ported body with disps baked for data_vm=0xE00000 + EINTR retry stub + zero filler; ~3596 meaningful bytes / ~200 lines).  hex1-darwin SHA 9823b612... matches.  phase1-hex1.nix is now `hex0 input output` + dd pad + sign.  (3) phase2-amd64-hex2.pl moved to legacy/ — replaced by hex0/sources/hex2_AMD64_darwin.hex0 (~2444 bytes / ~180 lines; hex2-darwin SHA 41e684b7... matches).  Build closure for phases 1-11 has zero perl, zero awk, zero python.  scripts/stage0/regen-hex0-sources.sh in repo for maintainer re-derivation when stage0Sources bumps; awk port scripts (m1-to-m0-syntax, port-{m0,catm,cc-arch}-darwin) are also maintainer-only — build-time consumes only the committed outputs in M2libc/amd64/ and hex0/sources/.
- 2026-05-26/27 (nixpkgs-style refactor): collapsed 64 phaseN-* files in `phases/` into 12 semantic per-package directories matching `~/Git/nixpkgs/pkgs/os-specific/linux/minimal-bootstrap/` layout (stage0-posix/, mescc-tools/, mes/, mescc-libc/, tinycc/, gnumake/, gnupatch/, coreutils/, bootstrap-deps/, gcc-4.6/, gcc-10/, gcc-latest/). Then inside each file: dropped dead aarch64 wrapper (-83 lines × 54 files), dropped empty nativeBuildInputs (-54 lines × 17 files), stripped `darwin-minimal-bootstrap-*-amd64` pname prefix/suffix (60 files), extracted `utils.nix:mkDarwin` helper for shared mkDerivation defaults (-101 lines × 17 files), hoisted smoke tests from buildPhase into proper `checkPhase` (12 files), converted all `args: with args; ...` headers to explicit callPackage-style `{ a, b, ..., ... }:` (60 files; trailing `...` keeps current orchestrator compatible). Final: 75 .nix files (vs nixpkgs's 80), gnu-hello-hash-comparison verified at the unchanged baseline `5019a64510837fae43fc7238b506ec11011542432c792b4ab7683db2e7ff2f73`. One regression caught + fixed: mkDarwin's hardcoded `dontUnpack=true` broke mes/source.nix which sets `src=mesTarball`; fix in utils.nix is `dontUnpack = !(attrs ? src)`. Maintainer helper scripts under scripts/refactor/{drop-isx86-wrapper.py,extract-smoke.py,explicit-args.py}.
- 2026-06-09: Repo cleanup: deleted the untracked cctools header symlink farms (bsd-extra/ cc-inc/ coff/ elf/ gnu/ i386/ llvm-c/ mach-o/ mach/ opcode/ standalone/ stuff/ xar/), tmp/bootstrap-as.* mktemp debris, and the gitignored stray *.h leaks at the root and under bake/ (kept bake/tarballs and bake/work).
- 2026-06-09: Re-measured the phase44 pure path: the chain cc1 (phase35, TCC-built) compiles combine.c in 125s and the worst generated file insn-recog.c (140k lines) in 243s. The old "~20h infeasible" estimate came from the serial default (PHASE44_MAIN_JOBS=1). Flipped phase44 to GCC46_BOOTSTRAP_HOST_CC_SOURCES=0 + GCC46_BOOTSTRAP_HOST_CC_GENERATED=0 + PHASE44_MAIN_JOBS=$NIX_BUILD_CORES: every GCC 4.6 source compiles with the chain cc1; nixpkgs clang remains only as the link driver (GCC46_BOOTSTRAP_MACHO_CC). rebuild in flight, gate = gnu-hello 0854f4ab… with phase46_phase47_equal=yes.
- 2026-06-09: phase45 + phase46 set GCC_MODERN_WRAPPER_HOST_SHORTCUTS=0 + GCC_MODERN_HOST_BUILD_CC=0 (the proven strict-phase47 configuration): build-helpers compile with the chain input compiler. phase45 adds GCC_MODERN_BUILD_TARGET_LIBS=1 so phase46's pure helpers get gcc-10's C++11 libstdc++ headers. Same gate.
- 2026-06-09: ROOT-CAUSED the phase46 phase39-gnumake ELOOP wall: bootstrap/tinycc-sysv-libc.c:459 getcwd() is a stub returning "." — phase39 make runs with CURDIR=., so $(abspath)/$(realpath) return inputs unchanged and libstdc++'s stamp-modules-bits rule ($(LN_S) $(abspath $?) . in include/bits) self-links std.cc → ELOOP at the cp -RL. Repro: printf 'all:\n\t@echo $(abspath foo)\n' | phase39 make → "./foo". Fix queued for wave 2 (full from-phase34 rebuild): implement getcwd via open(".")+fcntl(F_GETPATH).
- 2026-06-10: WAVE 1 VALIDATED — the gnu-hello gate reproduces 0854f4ab9cf255a37ddfb6251198164e6f14f3606239c963d2530f77e257f90a with phase46_phase47_equal=yes on the fully purified chain: phase44 compiles every GCC 4.6 source with the chain cc1 (c035d19), phase45 gcc-10 builds with chain helpers + from-stage0 libgcc/libstdc++ (8a1dad1), phase46 gcc-15 runs WRAPPER_HOST_SHORTCUTS=0 + HOST_BUILD_CC=0 like phase47. No host compiler compiles any chain source; nixpkgs clang/binutils stay at the assemble/link boundary, nixpkgs gnumake remains in phase44/46 (wave 2: libc getcwd fix + BOOTSTRAP_MAKE swaps + pname compression in one full from-seed rebuild).
