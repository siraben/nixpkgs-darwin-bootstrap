#!/bin/sh
## Shim: ELF-capable `ar` for the bake gcc build.  Apple's /usr/bin/ar
## refuses non-Mach-O members; bake-ar.py stores ELF objects verbatim.
d=$(cd "$(dirname "$0")" && pwd)
exec /usr/bin/python3 "$d/bake-ar.py" "$@"
