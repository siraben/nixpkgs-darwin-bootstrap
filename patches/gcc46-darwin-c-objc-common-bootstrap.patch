--- a/gcc/c-objc-common.c
+++ b/gcc/c-objc-common.c
@@ -21,1 +21,14 @@
+typedef void *tree;
+typedef const void *const_tree;
+typedef int bool;
+typedef void diagnostic_context;
+
+bool c_missing_noreturn_ok_p (tree decl) { (void) decl; return 1; }
+bool c_warn_unused_global_decl (const_tree decl) { (void) decl; return 0; }
+bool c_objc_common_init (void) { return 1; }
+bool has_c_linkage (const_tree decl) { (void) decl; return 1; }
+void c_initialize_diagnostics (diagnostic_context *context) { (void) context; }
+bool c_vla_unspec_p (tree x, tree fn) { (void) x; (void) fn; return 0; }
+
+#if 0
 #include "config.h"
@@ -212,3 +225,4 @@ c_vla_unspec_p (tree x, tree fn ATTRIBUTE_UNUSED)
 {
   return c_vla_type_p (x);
 }
+#endif
